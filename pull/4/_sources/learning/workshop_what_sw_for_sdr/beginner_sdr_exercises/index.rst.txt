Beginner SDR Exercises
======================

The following hands-on exercises demonstrate real-world software-defined radio applications using ADI's SDR platforms.
These exercises showcase the power of reconfigurable software running on specialized hardware, allowing you to implement
complex signal processing algorithms without custom hardware design.

.. note::

   The following exercises should be run on a Raspberry Pi 5 using the provided ADI Kuiper 2.0 Linux image.
   The Kuiper image contains all necessary software pre-installed, including Python, GNU Radio, Thonny IDE, and all required libraries.
   Connect your SDR hardware (Pluto, Jupiter, or Talise) to the Raspberry Pi 5 before starting the exercises.

.. TODO: Add actual download link when available
.. .. admonition:: Download Kuiper 2.0 Image
..    :class: download
..
..    Download the ADI Kuiper 2.0 image: `Kuiper 2.0 SD Card Image <PLACEHOLDER_URL>`_

Familiarize Yourself with the Tools
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Before diving into the exercises, we recommend familiarizing yourself with the SDR hardware and tools you'll be using. Understanding your hardware's capabilities, features, and interfaces will help you get the most out of these exercises.

**Explore the Hardware:**

* :doc:`ADALM-Pluto (PlutoSDR) </tools/pluto/index>` - Learn about the Pluto SDR platform, including hardware specifications, software drivers, and getting started guides.

These resources provide important information about:

* Hardware specifications and capabilities
* Supported frequency ranges and bandwidth
* Software drivers and libraries (PyADI-IIO, GNU Radio, etc.)
* Connectivity options and interfaces
* Calibration and performance characteristics

Taking time to understand your hardware will make the exercises more meaningful and help you troubleshoot issues more effectively.

.. _sinewave-loopback:

Transmit and receive a complex sinusoid (Sinewave Loopback)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

A complex sinusoid is a fundamental building block in software-defined radio. Unlike a real sinusoid
(which oscillates in a single dimension), a complex sinusoid consists of two components:

* **In-phase (I) component**: Real part of the signal
* **Quadrature (Q) component**: Imaginary part of the signal

Together, they form a single tone in the frequency domain. This exercise transmits a complex sinusoid
and receives it back via a loopback connection, allowing you to observe both the transmitted
signal and its frequency spectrum representation.

   .. figure:: images/exercises/sinewave_loopback/complex_sinusoid.png
      :alt: Complex Sinusoid
      :align: center
      :width: 40em

      Complex sinusoid with in-phase (I) and quadrature (Q) components

   .. figure:: images/exercises/sinewave_loopback/complex_sinusoid_constellation_plot.png
      :alt: Complex Sinusoid Constellation Plot
      :align: center
      :width: 40em

      Constellation plot of a complex sinusoid

The following subsections demonstrate different implementations of this exercise using various tools and platforms.

Using PyADI-IIO with Pluto
^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow the next steps:

#. Make the following setup using Pluto and connect it to your PC:

   .. figure:: images/exercises/sinewave_loopback/pluto_setup.png
      :alt: Pluto SDR Setup
      :align: center
      :width: 40em

      Pluto SDR setup with loopback connections

#. Download the SDR Lab Source Code from the FTC2023 workshop link and unzip it on your desktop.

#. Open **workshop_exercises** folder on your desktop.

#. Go to **python->sinewave_loopback** subfolder.

#. Right click on **python_loopback_sine_pluto.py -> Open with Other Application** and
   select **Thonny Python IDE** (or any other Python IDE you have installed) from the
   Recommended Applications list.

   .. figure:: images/exercises/sinewave_loopback/python_ide.png
      :alt: Open Python Script with IDE
      :align: center
      :width: 40em

      Open the Python script with Thonny Python IDE

#. To run the script, press the green round button from the top left corner in Thonny IDE as shown below.

   .. figure:: images/exercises/sinewave_loopback/run_script.png
      :alt: Run Python Script
      :align: center
      :width: 40em

      Run the Python script in Thonny IDE

#. After running the script, six plots appear:

   .. figure:: images/exercises/sinewave_loopback/tx_time_domain.png
      :alt: TX Time Domain
      :align: center
      :width: 40em

      TX time domain - I and Q components 90° out of phase.

   .. figure:: images/exercises/sinewave_loopback/tx_fft.png
      :alt: TX FFT
      :align: center
      :width: 40em

      TX FFT - single peak at 20 kHz.

   .. figure:: images/exercises/sinewave_loopback/tx_constellation_plot.png
      :alt: TX Constellation
      :align: center
      :width: 40em

      TX constellation - circular pattern from continuous phase rotation.

   .. figure:: images/exercises/sinewave_loopback/rx_time_domain.png
      :alt: RX Time Domain
      :align: center
      :width: 40em

      RX time domain - received I and Q components.

   .. figure:: images/exercises/sinewave_loopback/rx_fft.png
      :alt: RX FFT
      :align: center
      :width: 40em

      RX FFT - received spectrum with harmonic at 20 kHz.

   .. figure:: images/exercises/sinewave_loopback/rx_constellation_plot.png
      :alt: RX Constellation
      :align: center
      :width: 40em

      RX constellation - deviations from a perfect circle indicate channel effects.

#. To zoom on a plot, you can use the zoom option as depicted below. Encircle holding left click the area you want to zoom.
   On this example, if you zoom at the figure below on the received signal, you should see a harmonic at 20KHz.

   .. figure:: images/exercises/sinewave_loopback/zoom_plot.png
      :alt: Zoom on Plot
      :align: center
      :width: 40em

      Zoom on the plot to see the harmonic at 20KHz

#. To stop running the Python script (if you want to run it again or run another script), close all the tabs by **right clicking on the
   taskbar icon -> Quit x windows** as shown depicted below or close them manually one by one.

   .. figure:: images/exercises/sinewave_loopback/close_tabs.png
      :alt: Close Tabs
      :align: center
      :width: 40em

      Close all tabs to stop the Python script


Using PyADI-IIO with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Only the "Configure SDR" section changes compared to Pluto.

Follow these steps:

#. Go to **workshop_exercises→python→sinewave_loopback** and open **python_loopback_sine_jupiter.py**
   using **Thonny Python IDE**.

#. Observe that only the "Configure SDR" section changed compared to **python_loopback_sine_pluto.py**:

   .. code-block:: python
      :linenos:
      :lineno-start: 4
      :class: code-spaced

      # Configure SDR
      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio

      frequency = 15360  # 15.360 KHz sinusoid to be transmitted
      amplitude = 0.5
      center_freq = 915000000  # Hz
      sample_rate = sdr.rx0_sample_rate
      num_samps = int((sample_rate)/frequency)  # number of samples per call to rx()

      sdr.rx_ensm_mode_chan0 = "rf_enabled"
      sdr.tx_hardwaregain_chan0 = -5
      sdr.rx_hardwaregain_chan0 = 0
      sdr.tx_ensm_mode_chan0 = "rf_enabled"
      sdr.tx_cyclic_buffer = True
      sdr.tx_buffer_size = num_samps  # number of samples per call to tx()
      sdr.rx_buffer_size = num_samps
      sdr.tx0_lo = center_freq
      sdr.rx0_lo = center_freq

#. Insert the IP address on line 5 to remotely connect to Jupiter:

   .. code-block:: python
      :linenos:
      :lineno-start: 5
      :class: code-spaced

      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio object for Jupiter

#. Run the script by pressing the green button in Thonny. Close all plot windows to stop.

#. In this example, a sinusoid at 15.360 KHz is transmitted and received (chosen to have an integer
   number of periods at the sample rate of 15.36 Msps).


Using PyADI-IIO with Talise
^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. Go to **workshop_exercises→python→sinewave_loopback** and open **python_loopback_sine_talise_zu11eg.py**
   using **Thonny Python IDE**.

#. Make the following hardware setup with TX1A_P/N connected to RX1A_P/N via loopback cables.

   .. TODO: Add Talise hardware setup image when available
   .. .. figure:: images/exercises/sinewave_loopback_talise/talise_setup.png
   ..    :alt: Talise Hardware Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Talise hardware setup with loopback connections

#. Observe the "Configure SDR" section for Talise:

   .. code-block:: python
      :linenos:
      :lineno-start: 4
      :class: code-spaced

      # Configure properties
      sample_rate = 30720000  # Sample rate
      center_freq = 2000000000  # Center frequency
      num_samps = 100000  # Number of samples per call to rx()
      frequency = 3000000  # Frequency of complex sinusoid
      fc0 = int(center_freq / (sample_rate / 2) * 2**15)  # Digital frequency for TX1

      # Create radio object
      sdr = adi.adrv9009_zu11eg("ip:10.48.65.182")  # IP address of Talise

      # Configure Tx properties
      sdr.tx_hardwaregain_chan0 = -10  # TX attenuation in dB
      sdr.tx_enabled_channels = [0]
      sdr.dds_single_tone(fc0, 0.8, 0)  # Generate tone: freq, scale, channel

      # Configure Rx properties
      sdr.gain_control_mode_chan0 = "slow_attack"
      sdr.rx_enabled_channels = [0]
      sdr.rx_buffer_size = num_samps

#. Run the script by pressing the green button in Thonny. Close all plot windows to stop.

#. Observe the output plots showing transmitted and received signals.

   .. TODO: Add Talise output plots image when available
   .. .. figure:: images/exercises/sinewave_loopback_talise/talise_output.png
   ..    :alt: Talise Output Plots
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing transmitted and received signals


Using Scopy with Pluto
^^^^^^^^^^^^^^^^^^^^^^^

Scopy provides a visual interface for real-time signal analysis without writing code.
This exercise uses Scopy's instruments to transmit a test tone via DDS and observe the received spectrum.

Follow these steps:

#. Make the loopback setup using Pluto with TX1 connected to RX1 via an SMA cable and connect it to your PC.

   .. TODO: Add Scopy Pluto loopback setup image when available
   .. .. figure:: images/exercises/scopy_sinewave/pluto_scopy_setup.png
   ..    :alt: Pluto SDR Setup for Scopy
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Pluto SDR with loopback connection for Scopy analysis

#. Double-click the **Scopy** icon on the Desktop to open the application.

#. Click on the auto-detected USB device **AD936X**, then click **Connect**.

   .. TODO: Add Scopy device connection screenshot when available
   .. .. figure:: images/exercises/scopy_sinewave/scopy_device_connect.png
   ..    :alt: Scopy Device Connection
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Connecting to PlutoSDR in Scopy

#. Click on the **AD936X** instrument on the left sidebar and configure:

   * Set **RX LO Frequency** to **2450** MHz
   * Set **TX LO Frequency** to **2450** MHz
   * Set **RX1 Gain Control Mode** to **manual**
   * Set **RX2 Gain Control Mode** to **manual** (Rev C Pluto only)
   * Set **RX2 Gain** to **0** dB (Rev C Pluto only)
   * Set **TX2 Attenuation** to **-80** dB (Rev C Pluto only)

   .. TODO: Add Scopy AD936X plugin configuration when available
   .. .. figure:: images/exercises/scopy_sinewave/scopy_ad9361_config.png
   ..    :alt: Scopy AD936X Configuration
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Configuring RX/TX LO frequencies and gain settings

#. Click on the **DAC** instrument on the left sidebar:

   * Change mode to **DDS**
   * Set **DDS_MODE** to **One CW Tone**
   * Set **Frequency** to **0.089 MHz** (89 kHz)
   * Set **Scale** to **-10** dB

   .. TODO: Add Scopy DDS configuration when available
   .. .. figure:: images/exercises/scopy_sinewave/scopy_dds_config.png
   ..    :alt: Scopy DDS Configuration
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Configuring the DDS to generate an 89 kHz test tone

#. Click on the **ADC - Frequency** instrument on the left sidebar:

   * Click the **Complex** button on the bottom left
   * Set **FFT Size** to **65536** samples using the (+) button
   * Change **Window** to **No Window** in the right side menu
   * Click on **voltage2-voltage3** circle to disable the channel (Rev C Pluto only)

   .. TODO: Add Scopy ADC-Frequency configuration when available
   .. .. figure:: images/exercises/scopy_sinewave/scopy_adc_frequency.png
   ..    :alt: Scopy ADC-Frequency Configuration
   ..    :align: center
   ..    :width: 40em
   ..
   ..    ADC-Frequency instrument showing spectrum

#. Click on **voltage0-voltage1** channel to open Settings:

   * Enable the **MARKER** panel from the right side menu
   * Enable the **AVERAGING** panel and set to **20**

#. Click **Run** (or **Start**) and observe the spectrum plot showing the 89 kHz tone.

   .. TODO: Add Scopy spectrum output when available
   .. .. figure:: images/exercises/scopy_sinewave/scopy_spectrum_output.png
   ..    :alt: Scopy Spectrum Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Spectrum showing the 89 kHz test tone

#. Click on the **Measure** button (bottom right) and enable **Genalyzer analysis** and **Marker panel**
   to see detailed measurements.

   .. TODO: Add Scopy Genalyzer screenshot when available
   .. .. figure:: images/exercises/scopy_sinewave/scopy_genalyzer.png
   ..    :alt: Scopy Genalyzer Analysis
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Genalyzer analysis with detailed measurements

#. Go back to **AD936X** instrument and change the **TX Attenuation** (range is 0 to -89.5 dB).
   Observe the changes on the **ADC-Frequency** plot.

#. When done, click **Stop** (orange button) and close Scopy.

#. Reboot Pluto by unplugging and plugging back the USB connector.


Using GNU Radio with Pluto
^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise implements the sinewave loopback using GNU Radio Companion with interactive sliders
for real-time parameter adjustment.

Follow these steps:

#. Make the loopback setup using Pluto with TX connected to RX via an SMA cable and connect it to your PC.

   .. TODO: Add GNU Radio Pluto loopback setup image when available
   .. .. figure:: images/exercises/gnuradio_sinewave_pluto/pluto_gnuradio_setup.png
   ..    :alt: Pluto SDR Setup for GNU Radio
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Pluto SDR with loopback connection for GNU Radio

#. Open the terminal by pressing **Ctrl + Alt + T** and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open **sinewave_loopback_pluto.grc** from
   **Desktop → ftc_2025 → 2_sinewave_loopback_gnuradio**.

   .. TODO: Add GNU Radio flowgraph screenshot when available
   .. .. figure:: images/exercises/gnuradio_sinewave_pluto/gnuradio_flowgraph.png
   ..    :alt: GNU Radio Sinewave Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    GNU Radio flowgraph for sinewave loopback

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output plots showing transmitted/received samples, spectrum, and constellation diagram.
   Use the sliders to adjust parameters in real-time:

   * **Frequency slider**: Changes the offset frequency of the transmitted sinusoid
   * **RX Gain slider**: Controls receiver gain
   * **TX Attenuation slider**: Controls transmit power

   .. TODO: Add GNU Radio output plots screenshot when available
   .. .. figure:: images/exercises/gnuradio_sinewave_pluto/gnuradio_output.png
   ..    :alt: GNU Radio Sinewave Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output plots with interactive sliders for frequency, RX gain, and TX attenuation

#. Click the **Stop** button (red square) or close the window when finished.


Using GNU Radio with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise implements the same complex sinusoid loopback using GNU Radio Companion's visual flowgraph environment instead of Python code.

Follow these steps:

#. Make the following hardware setup using Jupiter SDR with TX1A connected to RX1A via loopback cable.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/gnuradio_sinewave_loopback/jupiter_setup.png
   ..    :alt: Jupiter SDR Hardware Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with TX1A connected to RX1A via loopback cable

#. Open the terminal and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open **Desktop → workshop_exercises → gnuradio → sinewave_loopback_jupiter.grc**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

   .. TODO: Add flowgraph execution screenshot when available
   .. .. figure:: images/exercises/gnuradio_sinewave_loopback/run_flowgraph.png
   ..    :alt: Run GNU Radio Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Click the green arrow to execute the flowgraph

#. Observe the output plots showing transmitted/received samples, spectrum, and constellation diagram.

   .. TODO: Add output plots screenshot when available
   .. .. figure:: images/exercises/gnuradio_sinewave_loopback/output_plots.png
   ..    :alt: GNU Radio Output Plots
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output plots showing transmitted/received samples, spectrum, and constellation diagram

#. Use the sliders to adjust parameters in real-time:

   * **Frequency slider**: Changes the offset frequency of the complex sinusoid
   * **TX Attenuation slider**: Controls transmit power
   * **RX Gain slider**: Controls receiver gain

#. Click the **Stop** button (red square) when finished.


Doppler Radar with GNU Radio (Pluto)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

The Doppler effect causes a frequency shift when there is relative motion between radar and target.
This exercise creates a continuous-wave radar using GNU Radio Companion.

The Pluto transmits a CW signal at 2.5 GHz. When the signal reflects off your moving hand,
the received frequency shifts - higher pitch when moving toward, lower when moving away.

   .. figure:: images/exercises/doppler_radar/filter_frequency_diagram.png
      :alt: Band Pass and Reject Filters
      :align: center
      :width: 40em

      Band Reject Filter removes carrier, Band Pass Filter isolates Doppler shift.

Follow these steps:

#. Make the following setup using Pluto and connect it to your PC:

   .. figure:: images/exercises/doppler_radar/pluto_doppler_setup.png
      :alt: Pluto SDR Doppler Radar Setup
      :align: center
      :width: 40em

      Pluto SDR setup for Doppler radar experiment

#. Open the terminal by simultaneously pressing **Ctrl + Alt + T** on your keyboard.

#. Type the following command in the terminal to open GNU Radio Companion and press Enter:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In *gnuradio-companion*, click on the **File->Open**, and open from **Desktop->workshop_exercises->gnuradio** the
   **doppler_radar_pluto.grc** file.

   .. figure:: images/exercises/doppler_radar/gnu_radio_doppler_schematic.png
      :alt: Doppler Radar GNU Radio Flowgraph
      :align: center
      :width: 40em

      GNU Radio flowgraph for the Doppler radar experiment showing signal processing blocks.

#. To run the flowgraph, press on the green arrow from the top function bar as indicated below.

   .. figure:: images/exercises/doppler_radar/run_grc.png
      :alt: Run GNU Radio Flowgraph
      :align: center
      :width: 40em

      Run the GNU Radio flowgraph for Doppler radar experiment

#. Move your hand toward and away from the antennas and observe how the sound and the frequency
   of the received harmonic changes in real time plots.

   The six plots show the received signal at different processing stages: top row displays
   time-domain waveforms, bottom row shows frequency-domain FFT plots where the Doppler shift
   peak moves left (hand moving away) or right (hand moving toward) from the center frequency.

   .. figure:: images/exercises/doppler_radar/doppler_plots.png
      :alt: Doppler Radar Plots
      :align: center
      :width: 40em

      Real-time plots of the Doppler radar experiment.


Binary Phase Shift Keying (BPSK)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

BPSK encodes digital data by shifting the carrier phase: bit 0 → -1, bit 1 → +1.
This creates two constellation points on the real axis.

   .. figure:: images/exercises/bpsk_loopback/bpsk_modulation_concept.png
      :alt: BPSK Modulation Concept
      :align: center
      :width: 40em

      BPSK modulation concept.

   .. figure:: images/exercises/bpsk_loopback/bpsk_constellation.png
      :alt: BPSK Constellation Diagram
      :align: center
      :width: 40em

      BPSK constellation - two points at -1 and +1.

Real-world reception introduces frequency offset, phase offset, and timing errors.
The receiver corrects these using:

* **Coarse frequency correction** - squares the signal to find 2× offset, then compensates
* **Symbol timing recovery** - Mueller and Muller algorithm selects optimal sample from each 16-sample symbol
* **Costas loop** - fine frequency and phase correction

   .. figure:: images/exercises/bpsk_loopback/tx_rx_flow_diagram.png
      :alt: BPSK TX/RX Flow
      :align: center
      :width: 40em

      BPSK transmitter and receiver signal processing chain.

   .. figure:: images/exercises/bpsk_loopback/bpsk_constellation_progression.png
      :alt: BPSK Signal Recovery Progression
      :align: center
      :width: 40em

      Constellation progression: raw → coarse freq → timing recovery → Costas loop.

Using PyADI-IIO with Pluto
^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps to run the BPSK transmission and reception experiment:

#. Make the setup using Pluto with loopback connection and connect it to your PC.

#. Go to **workshop_exercises→python→bpsk_loopback** and open **bpsk_pluto_loopback_pluto.py**
   using **Thonny Python IDE**.

#. Run the script by pressing the green button in Thonny. Close all plot windows to stop.

#. Twelve plots appear showing the BPSK signal processing stages:

   .. figure:: images/exercises/bpsk_loopback/bpsk_bits_transmitted.png
      :alt: BPSK Bits Transmitted
      :align: center
      :width: 40em

      Bits transmitted - original binary data.

   .. figure:: images/exercises/bpsk_loopback/bpsk_samples_transmitted.png
      :alt: BPSK Samples Transmitted
      :align: center
      :width: 40em

      Samples transmitted - 16 samples per symbol.

   .. figure:: images/exercises/bpsk_loopback/bpsk_tx_constellation_plot.png
      :alt: BPSK TX Constellation
      :align: center
      :width: 40em

      TX constellation - two points at -1 and +1.

   .. figure:: images/exercises/bpsk_loopback/bpsk_samples_received_raw.png
      :alt: BPSK Samples Received Raw
      :align: center
      :width: 40em

      Samples received raw - before any correction.

   .. figure:: images/exercises/bpsk_loopback/bpsk_rx_constellation_plot.png
      :alt: BPSK RX Constellation Raw
      :align: center
      :width: 40em

      RX constellation raw - scattered due to frequency and phase offset.

   .. figure:: images/exercises/bpsk_loopback/bpsk_coarse_freq_adj_samples.png
      :alt: BPSK Coarse Frequency Adjustment
      :align: center
      :width: 40em

      Samples after coarse frequency adjustment.

   .. figure:: images/exercises/bpsk_loopback/bpsk_rx_coarse_freq_constellation_plot.png
      :alt: BPSK RX Constellation After Coarse Freq
      :align: center
      :width: 40em

      RX constellation after coarse frequency correction - phase offset remains.

   .. figure:: images/exercises/bpsk_loopback/bspk_right_sample_selection.png
      :alt: BPSK Right Sample Selection
      :align: center
      :width: 40em

      Samples after timing recovery - selecting 1 of each 16 samples.

   .. figure:: images/exercises/bpsk_loopback/bpsk_rx_right_sample_constellation_plot.png
      :alt: BPSK RX Constellation Right Samples
      :align: center
      :width: 40em

      RX constellation after timing recovery - mid-constellation points eliminated.

   .. figure:: images/exercises/bpsk_loopback/bpsk_costas_loop_freq_vs_time.png
      :alt: BPSK Costas Loop Frequency vs Time
      :align: center
      :width: 40em

      Costas loop frequency error vs time - loop locking onto correct frequency.

   .. figure:: images/exercises/bpsk_loopback/bpsk_costas_loop_sample.png
      :alt: BPSK Costas Loop Samples
      :align: center
      :width: 40em

      Samples after Costas loop - fine frequency and phase correction applied.

   .. figure:: images/exercises/bpsk_loopback/bspk_rx_costas_loop_constellation_plot.png
      :alt: BPSK RX Constellation After Costas Loop
      :align: center
      :width: 40em

      RX constellation after Costas loop - clean clusters at -1 and +1.


Using PyADI-IIO with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Only the "Configure SDR" section changes compared to Pluto.

Follow these steps:

#. From **workshop_exercises→python→bpsk_loopback** subfolder, open **bpsk_jupiter_loopback_jupiter.py**
   using **Thonny Python IDE**.

#. Observe in the script that only the "Configure SDR" code section changed compared to **bpsk_pluto_loopback_pluto.py**:

   .. code-block:: python
      :linenos:
      :lineno-start: 4
      :class: code-spaced

      # Configure properties
      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio

      center_freq = 915000000  # Hz
      sample_rate = 30720000  # TX sample rate
      tx_buffer_size = 1024  # TX buffer_size

      sdr.rx_ensm_mode_chan0 = "rf_enabled"
      sdr.rx_ensm_mode_chan1 = "rf_enabled"
      sdr.tx_hardwaregain_chan0 = -20
      sdr.tx_hardwaregain_chan1 = 0
      sdr.rx_ensm_mode_chan0 = "rf_enabled"
      sdr.tx_cyclic_buffer = True
      sdr.tx_buffer_size = 1024  # number of samples per call to tx()
      sdr.tx0_lo = center_freq
      sdr.tx0_lo = center_freq
      sdr.rx0_lo = center_freq

      fs = int(sdr.rx0_sample_rate)

#. Insert the IP address on line 5 to remotely connect to Jupiter:

   .. code-block:: python
      :linenos:
      :lineno-start: 5
      :class: code-spaced

      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio object for Jupiter

#. Run the script by pressing the green button in Thonny. Close all plot windows to stop.


Using PyADI-IIO with Talise
^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. From **workshop_exercises→python→bpsk_loopback** subfolder, open **bpsk_talise_loopback_talise_zu11eg.py**
   using **Thonny Python IDE**.

#. Observe the "Configure SDR" section for Talise:

   .. code-block:: python
      :linenos:
      :lineno-start: 1
      :class: code-spaced

      # Configure properties
      sdr = adi.adrv9009_zu11eg("ip:10.48.65.182")  # Create Radio
      sdr.rx_enabled_channels = [0]
      sdr.tx_enabled_channels = [0]
      sdr.tx_cyclic_buffer = True
      sdr.tx_cyclic_buffer = True
      sdr.gain_control_mode_chan0 = "slow_attack"
      sdr.gain_control_mode_chan1 = "slow_attack"
      sdr.tx_hardwaregain_chan0 = -20
      sdr.tx_hardwaregain_chan1 = -20
      sdr.tx_hardwaregain_chan1_chip_b = -80
      sdr.tx_hardwaregain_chan1_chip_b = -80
      sdr.gain_control_mode_chan0 = "slow_attack"
      sdr.gain_control_mode_chan1 = "slow_attack"
      sdr.gain_control_mode_chan0_chip_b = "slow_attack"
      sdr.gain_control_mode_chan1_chip_b = "slow_attack"
      sdr.rx_buffer_size = 32768
      sdr.tx_buffer_size = 1024

      fs = int(sdr.rx_sample_rate)

#. Insert the IP address on line 2 to remotely connect to Talise:

   .. code-block:: python
      :linenos:
      :lineno-start: 2
      :class: code-spaced

      sdr = adi.adrv9009_zu11eg("ip:10.48.65.182")  # Create Radio object for Talise

#. Run the script by pressing the green button in Thonny. Close all plot windows to stop.


Quadrature Phase Shift Keying (QPSK)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

QPSK encodes 2 bits per symbol using four constellation points at 45°, 135°, 225°, and 315°.
This doubles spectral efficiency compared to BPSK.

   .. figure:: images/exercises/qpsk_gnuradio/qpsk_constellation.png
      :alt: QPSK Constellation
      :align: center
      :width: 40em

      QPSK constellation - 4 points, 2 bits/symbol.

   .. figure:: images/exercises/qpsk_gnuradio/qpsk_ho_modulation.png
      :alt: QPSK Higher Order Modulation
      :align: center
      :width: 40em

      Higher-order modulations: 16-QAM, 32-QAM, 64-QAM, 256-QAM.

Using GNU Radio with Pluto - Console Messaging
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Packet-based QPSK system where messages are transmitted in bursts and displayed on console.
Each packet contains: access key (preamble), payload length, message data, and CRC32 checksum.

   .. figure:: images/exercises/qpsk_gnuradio/qpsk_point_to_point_schematic.png
      :alt: QPSK Console Messaging Flowgraph
      :align: center
      :width: 40em

      Transmitter and receiver flowgraph for packet-based QPSK.

Follow these steps:

#. Make the following setup using Pluto and connect it to your PC:

   .. figure:: images/exercises/qpsk_pluto/qpsk_pluto_setup.png
      :alt: QPSK Pluto Setup
      :align: center
      :width: 40em

      QPSK Pluto Setup

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ gnuradio-companion

#. In GNU Radio Companion, open **console_message_receiver_pluto.grc** from
   **Desktop → ftc_2025 → 1_console_message_point_to_point_gnuradio → receiver_pluto**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output:

   .. figure:: images/exercises/qpsk_pluto/qpsk_point_to_point_rx_console.png
      :alt: QPSK Console Message Reception
      :align: center
      :width: 40em

      Console output with received message.

   .. figure:: images/exercises/qpsk_gnuradio/qpsk_point_to_point_output.png
      :alt: QPSK Signal Analysis Output
      :align: center
      :width: 40em

      Received samples, spectrum, and QPSK constellation.

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter - Console Messaging
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Same as Pluto, with minimal code changes for Jupiter.

Follow these steps:

#. Make the hardware setup using Jupiter SDR and connect it to your PC.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/qpsk_console/jupiter_setup.png
   ..    :alt: Jupiter SDR Hardware Setup for Console Messaging
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup for QPSK console messaging

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ gnuradio-companion

#. In GNU Radio Companion, open **console_message_receiver_jupiter.grc** from
   **Desktop → ftc_2025 → 1_console_message_point_to_point_gnuradio → receiver_jupiter**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the received message displayed in the **msg_rx variable** on the console, along with the constellation diagram showing the QPSK signal quality.

   .. TODO: Add Jupiter console output when available
   .. .. figure:: images/exercises/qpsk_console/jupiter_console_output.png
   ..    :alt: Jupiter QPSK Console Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter console output showing received message and signal analysis

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter - Without Additional DSP
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Intentionally omits matched filter, frequency correction, and timing recovery to show what happens
without them: wide spectrum, rotating constellation, wrong samples.

Follow these steps:

#. Make the hardware setup using Jupiter SDR with RX1 and TX1 connected via SMA loopback cable
   and connect it to your PC.

   .. TODO: Add Jupiter loopback setup image when available
   .. .. figure:: images/exercises/qpsk_raw/jupiter_loopback_setup.png
   ..    :alt: Jupiter SDR Loopback Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with RX1 and TX1 connected via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion
   with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **QPSK_raw_loopback_jupiter.grc** from
   **Desktop → ftc_2024 → 10_qpsk_raw_loopback_gnuradio**.

#. Observe the simplified flowgraph showing transmitter and receiver without DSP correction blocks.

   .. TODO: Add raw QPSK flowgraph image when available
   .. .. figure:: images/exercises/qpsk_raw/qpsk_raw_flowgraph.png
   ..    :alt: Simplified QPSK Flowgraph Without DSP
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Simplified QPSK flowgraph without synchronization and filtering blocks

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays. The transmitted spectrum shows wide square wave characteristics
   with high sidelobes. The received constellation displays scattered points with phase offset
   instead of clean clusters at the ideal symbol positions. The received spectrum demonstrates
   wide bandwidth due to the absence of pulse shaping.

   .. TODO: Add raw QPSK output comparison image when available
   .. .. figure:: images/exercises/qpsk_raw/qpsk_raw_output_comparison.png
   ..    :alt: QPSK Without DSP Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing inefficient spectrum and constellation rotation without DSP

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter - Constellation Modulator
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Uses Root-Raised Cosine (RRC) filters for pulse shaping, giving a cleaner spectrum than raw QPSK.
Still missing timing recovery and phase correction.

Follow these steps:

#. Make the hardware setup using Jupiter SDR with RX1 and TX1 connected via SMA loopback cable
   and connect it to your PC.

   .. TODO: Add Jupiter loopback setup image when available
   .. .. figure:: images/exercises/qpsk_constellation_mod/jupiter_loopback_setup.png
   ..    :alt: Jupiter SDR Loopback Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with RX1 and TX1 connected via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion
   with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **constellation_modulator_loopback_jupiter.grc** from
   **Desktop → ftc_2024 → 12_constellation_modulator_loopback_gnuradio**.

#. Observe the flowgraph structure showing the Constellation Modulator block and matched RRC filters.

   The Constellation Modulator block in the transmitter maps bytes into symbols and applies the RRC filter
   in a single operation. The receiver includes a separate RRC filter block to complete the matched filter pair.

   .. TODO: Add Constellation Modulator flowgraph image when available
   .. .. figure:: images/exercises/qpsk_constellation_mod/constellation_mod_flowgraph.png
   ..    :alt: QPSK Constellation Modulator Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    QPSK flowgraph with Constellation Modulator and matched RRC filters

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays and compare to the previous raw QPSK exercise.

   The transmitted spectrum shows significantly improved bandwidth efficiency with minimal sidelobes due to
   RRC pulse shaping. The received signal demonstrates better quality from the matched filter processing.
   However, the received signal is not yet decimated by selecting the optimal samples from each symbol period,
   which would be added in a complete receiver implementation.

   .. TODO: Add Constellation Modulator output image when available
   .. .. figure:: images/exercises/qpsk_constellation_mod/constellation_mod_output.png
   ..    :alt: QPSK Constellation Modulator Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing optimized bandwidth usage with matched RRC filters

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter - Frequency Locked Loop
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Adds a Frequency Locked Loop (FLL) to automatically correct for oscillator differences between
TX and RX, keeping the spectrum centered.

Follow these steps:

#. Make the hardware setup using Jupiter SDR with RX1 and TX1 connected via SMA loopback cable
   and connect it to your PC.

   .. TODO: Add Jupiter loopback setup image when available
   .. .. figure:: images/exercises/qpsk_fll/jupiter_loopback_setup.png
   ..    :alt: Jupiter SDR Loopback Setup for FLL
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with RX1 and TX1 connected via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion
   with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **FLL_loopback_jupiter.grc** from
   **Desktop → ftc_2024 → 13_fll_loopback_gnuradio**.

#. Observe the flowgraph structure showing the FLL Band-Edge block for frequency correction.

   The transmitter section includes the Constellation Modulator with RRC filtering. The receiver section
   now features the FLL Band-Edge filter block highlighted in the flowgraph, which dynamically adjusts
   the frequency of the received spectrum to keep it centered around DC.

   .. TODO: Add FLL flowgraph image when available
   .. .. figure:: images/exercises/qpsk_fll/fll_flowgraph.png
   ..    :alt: QPSK FLL Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    QPSK flowgraph with Frequency Locked Loop for coarse frequency correction

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays and use the frequency offset slider to adjust the offset between TX and RX.

   The transmitted spectrum shows the optimized bandwidth from RRC filtering. The received spectrum after
   the FLL block stays centered around DC even as you adjust the frequency offset slider, demonstrating
   the FLL's ability to track and correct frequency errors. The received constellation shows improved
   stability compared to implementations without frequency correction.

   Note that while the FLL keeps the spectrum centered, a more precise frequency offset correction using
   a Costas loop would further improve constellation quality. Additionally, the receiver does not yet
   perform symbol timing recovery to select optimal samples.

   .. TODO: Add FLL output image when available
   .. .. figure:: images/exercises/qpsk_fll/fll_output.png
   ..    :alt: QPSK FLL Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing FLL maintaining spectrum centered around DC despite frequency offset

#. Use the offset slider to introduce frequency offset and observe how the spectrum after the FLL block
   automatically adjusts to stay centered around DC.

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Pluto - QPSK Loopback with Full DSP
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Complete QPSK loopback demonstrating the full DSP chain: differential encoding, constellation modulation,
FLL for coarse frequency correction, Costas loop for fine phase correction, and eye diagrams.

Follow these steps:

#. Make the loopback setup using Pluto with TX connected to RX via an SMA cable and connect it to your PC.

#. Open the terminal by pressing **Ctrl + Alt + T** and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open **qpsk_loopback_pluto.grc** from
   **Desktop → ftc_2025 → 5_qpsk_loopback_gnuradio**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays:

   * **Transmitted Symbols (Before Constellation Modulator)**: Raw symbol pattern
   * **Received Demodulated Symbols**: Should match the transmitted pattern due to differential decoding
   * **Transmitted Samples (After Constellation Modulator)**: Pulse-shaped I/Q samples
   * **Received Samples (After Costas Loop)**: Time-domain I/Q after carrier recovery
   * **Transmitted Spectrum**: Shows the RRC-filtered signal bandwidth
   * **Received Spectrum (After FLL)**: Spectrum centered around DC by the FLL
   * **Received Constellation**: QPSK constellation after carrier recovery
   * **Eye Diagram (Rx Re/Im After Costas Loop)**: Shows symbol timing and signal quality

   .. TODO: Add QPSK loopback output screenshot when available
   .. .. figure:: images/exercises/qpsk_loopback_pluto/qpsk_loopback_output.png
   ..    :alt: QPSK Loopback Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Complete QPSK loopback output showing matched TX/RX symbols, spectrum, constellation, and eye diagram

#. Use the **constellation rotation slider** to rotate the constellation in 90° steps. Observe that the
   received demodulated symbols remain the same due to **differential decoding** - demonstrating how
   differential encoding makes the system immune to 90° phase ambiguity.

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Pluto - Packet-Based File Storage
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Same as Console Messaging, but saves decoded messages to a file instead of displaying on console.

Follow these steps:

#. Make the hardware setup using Pluto and connect it to your PC.

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **qpsk_point_to_point_rx_file.grc** from **workshop_exercises->gnuradio->qpsk_point_to_point_file**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

   .. TODO: Add flowgraph and output screenshots when available

#. Observe the received message in the GUI. The decoded message is saved to a file in the project directory.

#. Click the **Stop** button (red square) when finished.


.. TODO: SDRangel demo commented out - needs verification
.. Using SDRangel with Pluto - DATV Video Reception
.. ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
..
.. This exercise demonstrates a real-world application of QPSK modulation: receiving Digital Amateur Television (DATV). Using the open-source `SDRangel <https://www.sdrangel.org/>`_ software, you will receive and display live video transmitted over the air using QPSK modulation, the same technique used in satellite TV broadcasting.
..
.. **What is DATV?**
..
.. Digital Amateur Television (DATV) is a form of amateur radio that transmits video and audio using digital modulation schemes like QPSK. Unlike analog TV, DATV provides:
..
.. * Better quality with less interference
.. * More efficient use of bandwidth
.. * Error correction capabilities
.. * Multiple audio/video streams in a single channel
..
.. **How This Exercise Works:**
..
.. `SDRangel <https://www.sdrangel.org/>`_ is a powerful open-source SDR application that implements a complete DATV receiver chain including QPSK demodulation, Forward Error Correction (FEC), and MPEG-2/H.264 video decoding. The Pluto SDR receives the RF signal, and SDRangel processes it to display the video in real-time.
..
.. Follow these steps to receive QPSK-modulated video:
..
.. #. Make the setup using Pluto and connect it to your PC:
..
..    .. figure:: images/exercises/qpsk_pluto/qpsk_pluto_setup.png
..       :alt: QPSK Pluto Setup
..       :align: center
..       :width: 40em
..
..       QPSK Pluto Setup
..
.. #. Open the terminal by pressing at the same time **Ctrl + Alt + T** on your keyboard.
..
.. #. Type the following command in the terminal and press Enter:
..
..    .. shell::
..       :user: analog
..       :group: analog
..       :show-user:
..
..       $ sdrangel
..
.. #. The application should be preconfigured. Run the program by pressing the purple arrow as depicted below.
..
..    .. figure:: images/exercises/qpsk_video/sdrangel_run.png
..       :alt: Run SDRangel
..       :align: center
..       :width: 40em
..
..       Run SDRangel to receive QPSK modulated video
..
.. #. On DATV window, **check and then uncheck Viterbi** to synchronize the qpsk data **while the program is running** (as shown below).
..
..    .. figure:: images/exercises/qpsk_video/viterbi_sync.png
..       :alt: Viterbi Sync
..       :align: center
..       :width: 40em
..
..       Check and then uncheck Viterbi to synchronize the QPSK data in SDRangel

Spectrum Paint using Pluto
~~~~~~~~~~~~~~~~~~~~~~~~~~

This exercise demonstrates a creative application of SDR: "painting" images in the frequency spectrum. By transmitting signals at different frequencies over time, you can create visible patterns on a waterfall display, which shows frequency on the horizontal axis and time on the vertical axis.

**How Spectrum Paint Works:**

A waterfall display is a spectrogram that shows:

* **Frequency** (horizontal axis): Different colors represent signal strength at each frequency
* **Time** (vertical axis): Each new row represents a new time snapshot
* **Amplitude** (color intensity): Brighter colors indicate stronger signals

By carefully controlling which frequencies are transmitted at each moment, you can "draw" images, text, or patterns that become visible as the waterfall scrolls down.

**Applications:**

While spectrum paint is primarily a demonstration, the underlying technique is used in:

* Frequency-hopping spread spectrum (FHSS)
* Chirp radar systems
* Multi-carrier communications
* Signal identification and watermarking

This exercise showcases the flexibility of software-defined radio: the same hardware can transmit any arbitrary frequency pattern by simply changing the software.

Follow these steps to create a spectrum painting:

#. Make the following setup using Pluto and connect it to your PC.

#. Open the terminal and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open the **paint_tx.grc** file from **workshop_exercises->gnuradio->spectrum_paint**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the waterfall display as the spectrum painting appears, created by the Pluto SDR transmitting the frequency pattern.

   .. figure:: images/exercises/spectrum_paint/paint_tx.png
      :alt: Spectrum Paint Waterfall Display
      :align: center
      :width: 40em

      Waterfall display showing the spectrum painting created by transmitting controlled frequency patterns

Amplitude Shift Keying (ASK) in GNU Radio
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

ASK encodes data by varying signal amplitude. This exercise uses 4-ASK (levels 0, 1/3, 2/3, 1).
Receiver extracts magnitude only (discards phase), then decimates to recover symbols.

Combining ASK with PSK gives QAM - used in Wi-Fi, LTE, 5G, cable modems.


Using GNU Radio with Pluto
^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. Make the loopback setup using Pluto with TX connected to RX via an SMA cable and connect it to your PC.

   .. TODO: Add Pluto hardware setup image when available
   .. .. figure:: images/exercises/ask_pluto/pluto_ask_setup.png
   ..    :alt: Pluto SDR Setup for ASK
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Pluto SDR with loopback connection for ASK

#. Open the terminal by pressing **Ctrl + Alt + T** and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open **ASK_loopback_pluto.grc** from
   **Desktop → ftc_2025 → 6_ask_loopback_gnuradio**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays:

   * **Transmitted Samples**: Shows the repeated symbols with 100 samples per symbol (levels 0, 1/3, 2/3, 1)
   * **Received Samples**: Raw I/Q signal from the receiver showing amplitude variations
   * **Received Spectrum**: Frequency-domain representation
   * **Received Constellation**: Shows **4 distinct amplitude levels** on the real axis
   * **Magnitude RX**: Time-domain plot showing only the amplitude (magnitude) of the received signal
   * **Decimated Magnitude RX**: After keeping 1 in 100 samples, showing the recovered symbol sequence

   .. TODO: Add ASK Pluto output screenshot when available
   .. .. figure:: images/exercises/ask_pluto/ask_pluto_output.png
   ..    :alt: ASK Pluto Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    ASK output showing TX symbols, RX constellation with 4 amplitude levels, and magnitude plots

#. Use the frequency offset slider to change the TX signal relative to the LO frequency
   and observe the changes in the spectrum display.

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. Make the hardware setup using Jupiter SDR with TX1A connected to RX1A via loopback cable and connect it to your PC.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/ask_gnuradio/jupiter_setup.png
   ..    :alt: Jupiter SDR Hardware Setup for ASK
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with TX1A connected to RX1A via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **ASK_loopback_jupiter.grc** from **Desktop->ftc_2024->8_ask_loopback_gnuradio**.

#. Observe the flowgraph structure showing the transmitter and receiver chains:

   .. TODO: Add flowgraph diagram when available
   .. .. figure:: images/exercises/ask_gnuradio/ask_flowgraph.png
   ..    :alt: ASK Loopback Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    GNU Radio flowgraph showing ASK transmitter and receiver signal processing

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays:

   * **Transmitted Samples**: Shows the repeated symbols with 100 samples per symbol
   * **Received Samples (Unprocessed)**: Raw I/Q signal from the receiver showing amplitude variations
   * **Received Spectrum**: Frequency-domain representation showing the offset carrier
   * **Received Constellation**: Shows **4 distinct amplitude levels** on the real axis (0, 1/3, 2/3, 1)
   * **Magnitude RX**: Time-domain plot showing only the amplitude of the received signal
   * **Decimated RX**: After keeping 1 in 100 samples, showing the recovered symbol sequence

   .. TODO: Add output screenshot when available
   .. .. figure:: images/exercises/ask_gnuradio/ask_output.png
   ..    :alt: ASK Output Plots
   ..    :align: center
   ..    :width: 40em
   ..
   ..    ASK signal processing output showing transmitted symbols, received constellation, and magnitude plots

#. You can adjust the frequency offset slider to observe how the received signal changes in the spectrum display.

#. Click the **Stop** button (red square) when finished.


Frequency Shift Keying (FSK) in GNU Radio
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

FSK encodes data by switching between different frequencies. This exercise uses 4-FSK.
Receiver uses quadrature demodulation to convert frequency changes to amplitude, then decimates.

FSK variants: GFSK (Bluetooth), Chirp Spread Spectrum (LoRa), OFDM subcarriers (Wi-Fi, LTE).


Using GNU Radio with Pluto
^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. Make the loopback setup using Pluto with TX connected to RX via an SMA cable and connect it to your PC.

   .. TODO: Add Pluto hardware setup image when available
   .. .. figure:: images/exercises/fsk_pluto/pluto_fsk_setup.png
   ..    :alt: Pluto SDR Setup for FSK
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Pluto SDR with loopback connection for FSK

#. Open the terminal by pressing **Ctrl + Alt + T** and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open **FSK_loopback_pluto.grc** from
   **Desktop → ftc_2025 → 7_fsk_loopback_gnuradio**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays:

   * **Transmitted Symbols**: Shows the repeated symbol values that control the VCO
   * **Transmitted Spectrum**: Frequency-domain showing **4 distinct frequency peaks** for the 4 FSK tones
   * **Received Spectrum**: Should match the transmitted spectrum
   * **Waterfall Display**: Time-frequency representation showing frequency hopping pattern
   * **Demodulated Output**: After quadrature demod and decimation, showing the recovered symbol sequence

   .. TODO: Add FSK Pluto output screenshot when available
   .. .. figure:: images/exercises/fsk_pluto/fsk_pluto_output.png
   ..    :alt: FSK Pluto Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    FSK output showing spectrum with 4 frequency peaks and waterfall display

#. Adjust the frequency offset slider to observe the changes in the spectrum display.

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. Make the hardware setup using Jupiter SDR with TX1A connected to RX1A via loopback cable and connect it to your PC.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/fsk_gnuradio/jupiter_fsk_setup.png
   ..    :alt: Jupiter SDR Hardware Setup for FSK
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with TX1A connected to RX1A via loopback cable for FSK experiment

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **FSK_loopback_jupiter.grc** from **Desktop->ftc_2024->9_fsk_loopback_gnuradio**.

#. Observe the flowgraph structure showing the transmitter and receiver chains:

   .. TODO: Add flowgraph diagram when available
   .. .. figure:: images/exercises/fsk_gnuradio/fsk_flowgraph.png
   ..    :alt: FSK Loopback Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    GNU Radio flowgraph showing FSK transmitter with VCO and receiver with quadrature demodulator

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays:

   * **Transmitted Symbols**: Shows the repeated symbol values that control the VCO
   * **Transmitted Spectrum**: Frequency-domain showing **4 distinct frequency peaks** for the 4 FSK tones
   * **Waterfall Display**: Time-frequency representation showing frequency hopping pattern
   * **Received Spectrum**: Should match the transmitted spectrum showing the 4 FSK tones
   * **Received Time Domain**: I/Q waveform showing frequency transitions
   * **Demodulated Output**: After quadrature demod and decimation, showing the recovered symbol sequence

   .. TODO: Add output screenshot when available
   .. .. figure:: images/exercises/fsk_gnuradio/fsk_spectrum_output.png
   ..    :alt: FSK Spectrum with Multiple Frequency Peaks
   ..    :align: center
   ..    :width: 40em
   ..
   ..    FSK signal spectrum showing four distinct frequency peaks corresponding to the four transmitted tones

#. You can adjust the frequency deviation slider (if available) to observe how the spacing between frequency peaks changes.

#. Observe the waterfall display to see the frequency transitions over time:

   .. TODO: Add waterfall screenshot when available
   .. .. figure:: images/exercises/fsk_gnuradio/fsk_waterfall.png
   ..    :alt: FSK Waterfall Display
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Waterfall display showing FSK frequency transitions over time as the transmitter cycles through the four tones

#. Click the **Stop** button (red square) when finished.
