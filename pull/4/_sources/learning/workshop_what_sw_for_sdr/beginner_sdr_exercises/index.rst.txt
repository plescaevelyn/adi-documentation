Beginner SDR Exercises
======================

The following hands-on exercises demonstrate real-world software-defined radio applications using ADI's SDR platforms.
These exercises showcase the power of reconfigurable software running on specialized hardware, allowing you to implement
complex signal processing algorithms without custom hardware design.

.. note::

   The following exercises should be run on a Raspberry Pi 5 using the provided ADI Kuiper 2.0 Linux image.
   The Kuiper image contains all necessary software pre-installed, including Python, GNU Radio, Thonny IDE, and all required libraries.
   Connect your SDR hardware (Pluto, Jupiter, or Talise) to the Raspberry Pi 5 before starting the exercises.

.. TODO: Add actual download link when available
.. .. admonition:: Download Kuiper 2.0 Image
..    :class: download
..
..    Download the ADI Kuiper 2.0 image: `Kuiper 2.0 SD Card Image <PLACEHOLDER_URL>`_

Transmit and receive a complex sinusoid (Sinewave Loopback)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

A complex sinusoid is a fundamental building block in software-defined radio. Unlike a real sinusoid
(which oscillates in a single dimension), a complex sinusoid consists of two components:

* **In-phase (I) component**: Real part of the signal
* **Quadrature (Q) component**: Imaginary part of the signal

Together, they form a single tone in the frequency domain. This exercise transmits a complex sinusoid
and receives it back via a loopback connection, allowing you to observe both the transmitted
signal and its frequency spectrum representation.

   .. figure:: images/exercises/sinewave_loopback/complex_sinusoid.png
      :alt: Complex Sinusoid
      :align: center
      :width: 40em

      Complex sinusoid with in-phase (I) and quadrature (Q) components

   .. figure:: images/exercises/sinewave_loopback/complex_sinusoid_constellation_plot.png
      :alt: Complex Sinusoid Constellation Plot
      :align: center
      :width: 40em

      Constellation plot of a complex sinusoid

The following subsections demonstrate different implementations of this exercise using various tools and platforms.

Using PyADI-IIO with Pluto
^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow the next steps:

#. Make the following setup using Pluto and connect it to your PC:

   .. figure:: images/exercises/sinewave_loopback/pluto_setup.png
      :alt: Pluto SDR Setup
      :align: center
      :width: 40em

      Pluto SDR setup with loopback connections

#. Download the SDR Lab Source Code from the FTC2023 workshop link and unzip it on your desktop.

#. Open **workshop_exercises** folder on your desktop.

#. Go to **python->sinewave_loopback** subfolder.

#. Right click on **python_loopback_sine_pluto.py -> Open with Other Application** and
   select **Thonny Python IDE** (or any other Python IDE you have installed) from the
   Recommended Applications list.

   .. figure:: images/exercises/sinewave_loopback/python_ide.png
      :alt: Open Python Script with IDE
      :align: center
      :width: 40em

      Open the Python script with Thonny Python IDE

#. To run the script, press the green round button from the top left corner in Thonny IDE as shown below.

   .. figure:: images/exercises/sinewave_loopback/run_script.png
      :alt: Run Python Script
      :align: center
      :width: 40em

      Run the Python script in Thonny IDE

#. To zoom on a plot, you can use the zoom option as depicted below. Encircle holding left click the area you want to zoom.
   On this example, if you zoom at the figure below on the received signal, you should see a harmonic at 20KHz.

   .. figure:: images/exercises/sinewave_loopback/zoom_plot.png
      :alt: Zoom on Plot
      :align: center
      :width: 40em

      Zoom on the plot to see the harmonic at 20KHz

#. To stop running the Python script (if you want to run it again or run another script), close all the tabs by **right clicking on the
   taskbar icon -> Quit x windows** as shown depicted below or close them manually one by one.

   .. figure:: images/exercises/sinewave_loopback/close_tabs.png
      :alt: Close Tabs
      :align: center
      :width: 40em

      Close all tabs to stop the Python script


Using PyADI-IIO with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This demonstrates **platform portability**—running the same algorithm on higher-performance hardware with minimal code changes.

Follow these steps:

#. Go to **workshop_exercises→python→sinewave_loopback** and open **python_loopback_sine_jupiter.py** using **Thonny Python IDE**.

#. Observe that only the "Configure SDR" section changed compared to **python_loopback_sine_pluto.py**:

   .. code-block:: python
      :linenos:
      :lineno-start: 4
      :class: code-spaced

      # Configure SDR
      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio

      frequency = 15360  # 15.360 KHz sinusoid to be transmitted
      amplitude = 0.5
      center_freq = 915000000  # Hz
      sample_rate = sdr.rx0_sample_rate
      num_samps = int((sample_rate)/frequency)  # number of samples per call to rx()

      sdr.rx_ensm_mode_chan0 = "rf_enabled"
      sdr.tx_hardwaregain_chan0 = -5
      sdr.rx_hardwaregain_chan0 = 0
      sdr.tx_ensm_mode_chan0 = "rf_enabled"
      sdr.tx_cyclic_buffer = True
      sdr.tx_buffer_size = num_samps  # number of samples per call to tx()
      sdr.rx_buffer_size = num_samps
      sdr.tx0_lo = center_freq
      sdr.rx0_lo = center_freq

#. Insert the IP address on line 5 to remotely connect to Jupiter:

   .. code-block:: python
      :linenos:
      :lineno-start: 5
      :class: code-spaced

      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio object for Jupiter

#. To run the script, press the green round button from the top left corner in Thonny IDE.

#. In this example, a sinusoid at 15.360 KHz is transmitted and received (chosen to have an integer
   number of periods at the sample rate of 15.36 Msps).

#. To stop running the Python script (if you want to run it again or run another script), close all the tabs by **right clicking on the
   taskbar icon -> Quit x windows** or close them manually one by one.


Using PyADI-IIO with Talise
^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. Go to **workshop_exercises→python→sinewave_loopback** and open **python_loopback_sine_talise_zu11eg.py** using **Thonny Python IDE**.

#. Make the following hardware setup with TX1A_P/N connected to RX1A_P/N via loopback cables.

   .. TODO: Add Talise hardware setup image when available
   .. .. figure:: images/exercises/sinewave_loopback_talise/talise_setup.png
   ..    :alt: Talise Hardware Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Talise hardware setup with loopback connections

#. Observe the "Configure SDR" section for Talise:

   .. code-block:: python
      :linenos:
      :lineno-start: 4
      :class: code-spaced

      # Configure properties
      sample_rate = 30720000  # Sample rate
      center_freq = 2000000000  # Center frequency
      num_samps = 100000  # Number of samples per call to rx()
      frequency = 3000000  # Frequency of complex sinusoid
      fc0 = int(center_freq / (sample_rate / 2) * 2**15)  # Digital frequency for TX1

      # Create radio object
      sdr = adi.adrv9009_zu11eg("ip:10.48.65.182")  # IP address of Talise

      # Configure Tx properties
      sdr.tx_hardwaregain_chan0 = -10  # TX attenuation in dB
      sdr.tx_enabled_channels = [0]
      sdr.dds_single_tone(fc0, 0.8, 0)  # Generate tone: freq, scale, channel

      # Configure Rx properties
      sdr.gain_control_mode_chan0 = "slow_attack"
      sdr.rx_enabled_channels = [0]
      sdr.rx_buffer_size = num_samps

#. To run the script, press the green round button from the top left corner in Thonny IDE.

#. Observe the output plots showing transmitted and received signals.

   .. TODO: Add Talise output plots image when available
   .. .. figure:: images/exercises/sinewave_loopback_talise/talise_output.png
   ..    :alt: Talise Output Plots
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing transmitted and received signals

#. To stop running the Python script (if you want to run it again or run another script), close all the tabs by **right clicking on the
   taskbar icon -> Quit x windows** or close them manually one by one.


Using GNU Radio with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise implements the same complex sinusoid loopback using GNU Radio Companion's visual flowgraph environment instead of Python code.

Follow these steps:

#. Make the following hardware setup using Jupiter SDR with TX1A connected to RX1A via loopback cable.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/gnuradio_sinewave_loopback/jupiter_setup.png
   ..    :alt: Jupiter SDR Hardware Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with TX1A connected to RX1A via loopback cable

#. Open the terminal and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open **Desktop → workshop_exercises → gnuradio → sinewave_loopback_jupiter.grc**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

   .. TODO: Add flowgraph execution screenshot when available
   .. .. figure:: images/exercises/gnuradio_sinewave_loopback/run_flowgraph.png
   ..    :alt: Run GNU Radio Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Click the green arrow to execute the flowgraph

#. Observe the output plots showing transmitted/received samples, spectrum, and constellation diagram.

   .. TODO: Add output plots screenshot when available
   .. .. figure:: images/exercises/gnuradio_sinewave_loopback/output_plots.png
   ..    :alt: GNU Radio Output Plots
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output plots showing transmitted/received samples, spectrum, and constellation diagram

#. Use the sliders to adjust parameters in real-time:

   * **Frequency slider**: Changes the offset frequency of the complex sinusoid
   * **TX Attenuation slider**: Controls transmit power
   * **RX Gain slider**: Controls receiver gain

#. Click the **Stop** button (red square) when finished.


Doppler Radar with GNU Radio (Pluto)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

This exercise demonstrates the Doppler radar effect using GNU Radio Companion, a visual programming environment
for building software-defined radio applications. You will create a continuous-wave (CW) radar system that can
detect motion and measure relative velocity by observing frequency shifts in the received signal.

**What is GNU Radio?**

GNU Radio is a free, open-source software development toolkit that provides signal processing blocks to implement
software-defined radios. It uses a visual flowgraph paradigm where signal processing blocks are connected together
to create complete radio systems. GNU Radio Companion (GRC) provides a graphical interface for building these flowgraphs
without requiring extensive programming knowledge.

**Key features:**

* Visual flowgraph-based development environment
* Extensive library of signal processing blocks
* Hardware integration via gr-iio blocks for ADI devices
* Real-time visualization and debugging tools
* Python-based backend for custom block development

**The Doppler Effect in Radar**

The Doppler effect causes a frequency shift when there is relative motion between the radar and a target.
When an object moves toward the radar, the reflected signal has a higher frequency (positive Doppler shift).
When it moves away, the frequency decreases (negative Doppler shift).

**Doppler Frequency Shift:**

.. math::

   f_d = \frac{2 \cdot v \cdot f_c}{c}

where:

* :math:`f_d` = Doppler frequency shift
* :math:`v` = relative velocity of the target
* :math:`f_c` = carrier frequency
* :math:`c` = speed of light

**How This Exercise Works:**

#. **Transmit**: The Pluto SDR transmits a continuous wave (CW) signal at 2.5 GHz
#. **Reflection**: The signal reflects off your moving hand
#. **Receive**: The Pluto receives the reflected signal with a Doppler-shifted frequency
#. **Processing**: GNU Radio processes the signal to extract the Doppler shift
#. **Visualization**: Real-time plots show the frequency shift, and audio output lets you hear the Doppler tone

   .. figure:: images/exercises/doppler_radar/filter_frequency_diagram.png
      :alt: Band Pass and Reject Filters
      :align: center
      :width: 40em

      Frequency domain filtering: Band Reject Filter removes carrier, Band Pass Filter isolates Doppler shift

**What You Will Observe:**

* Moving your hand toward the antenna increases the frequency (higher pitch sound)
* Moving away decreases the frequency (lower pitch sound)
* The faster the motion, the larger the frequency shift
* Real-time FFT plots visualize the Doppler shift

This is the same principle used in police speed radar guns, weather radar, and automotive collision avoidance systems.

Follow these steps to run the Doppler radar experiment:

#. Make the following setup using Pluto and connect it to your PC:

   .. figure:: images/exercises/doppler_radar/pluto_doppler_setup.png
      :alt: Pluto SDR Doppler Radar Setup
      :align: center
      :width: 40em

      Pluto SDR setup for Doppler radar experiment

#. Open the terminal by simultaneously pressing **Ctrl + Alt + T** on your keyboard.

#. Type the following command in the terminal to open GNU Radio Companion and press Enter:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In *gnuradio-companion*, click on the **File->Open**, and open from **Desktop->workshop_exercises->gnuradio** the
**doppler_radar_pluto.grc** file.

#. To run the flowgraph, press on the grey arrow from the top function bar as indicated below.

   .. figure:: images/exercises/doppler_radar/run_grc.png
      :alt: Run GNU Radio Flowgraph
      :align: center
      :width: 40em

      Run the GNU Radio flowgraph for Doppler radar experiment

#. Move your hand toward and away from the antennas and observe how the sound and the frequency of the received harmonic changes
in real time plots.

The six plots show the received signal at different processing stages: top row displays time-domain waveforms,
while the bottom row shows frequency-domain FFT plots where you can see the Doppler shift peak move left (hand moving away) or
right (hand moving toward) from the center frequency.

   .. figure:: images/exercises/doppler_radar/doppler_plots.png
      :alt: Doppler Radar Plots
      :align: center
      :width: 40em

      Real-time plots of the Doppler radar experiment


Binary Phase Shift Keying (BPSK)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

This exercise demonstrates Binary Phase Shift Keying (BPSK), a fundamental digital modulation technique used in modern communication
systems. You will transmit digital data (binary bits) through SDR platforms, receive it back via a loopback connection, and observe
how software-based signal processing recovers the original data despite real-world impairments.

**What is BPSK?**

Binary Phase Shift Keying is a type of phase modulation where digital information is encoded by shifting the phase of a carrier signal.
In BPSK:

* **Bit 0** is represented by a phase of 180° (or -1 in the I/Q plane)
* **Bit 1** is represented by a phase of 0° (or +1 in the I/Q plane)

This creates two distinct constellation points on the real axis of the I/Q diagram, making BPSK one of the most robust digital
modulation schemes against noise.

   .. figure:: images/exercises/bpsk_loopback/bpsk_modulation_concept.png
      :alt: BPSK Modulation Concept
      :align: center
      :width: 40em

      BPSK modulation: binary bits modulate the phase of the carrier signal

   .. figure:: images/exercises/bpsk_loopback/bpsk_constellation.png
      :alt: BPSK Constellation Diagram
      :align: center
      :width: 40em

      BPSK constellation diagram showing two points at -1 and +1 on the real axis

**Where is BPSK Used?**

BPSK and its variants are used in numerous communication systems:

* **GSM** - Mobile phone networks
* **Satellite Television** - DVB-S and other satellite broadcasting standards
* **Wi-Fi** - Lower data rate modes (802.11b DSSS)
* **GPS and GNSS** - Satellite navigation systems
* **RFID and NFC** - Contactless communication
* **Deep space communications** - Where robustness is critical

**Challenges in Demodulating BPSK Signals**

When receiving BPSK signals in real-world conditions, several impairments must be corrected:

* **Phase Offset**: The local oscillator (LO) at the receiver may have a phase difference from the transmitter
* **Frequency Offset**: Small differences in carrier frequency between TX and RX oscillators
* **Timing Offset**: Clock differences require symbol timing recovery to sample at the right moment
* **Variation with time, distance, and temperature**: These offsets change dynamically

As demonstrated in the previous Doppler radar exercise, frequency varies with distance and motion. These same effects impact digital communications.

**The Power of Software-Defined Radio: All These Problems Can Be Solved by Software!**

This exercise demonstrates how modern SDR systems correct all these impairments in software using sophisticated algorithms:

   .. figure:: images/exercises/bpsk_loopback/tx_rx_flow_diagram.png
      :alt: BPSK Transmitter and Receiver Architecture
      :align: center
      :width: 40em

      BPSK system architecture showing transmitter and receiver signal processing chain

**How the BPSK System Works:**

**Transmitter Chain:**

#. **Configure SDR**: Set center frequency, sample rate, and gain
#. **Create array of bits**: Generate the digital data to transmit (0s and 1s)
#. **Interpolate and remap symbols**: Repeat each bit 16 times for pulse shaping, map bit 0 → -1, bit 1 → +1
#. **Call tx() function**: Transmit the modulated signal

**Receiver Chain:**

#. **Call rx() function**: Receive the modulated signal
#. **Coarse frequency adjustment**: Square the received signal to measure 2× frequency offset, then correct it
#. **Symbol timing recovery**: Use Mueller and Muller clock recovery to select the correct sample from each 16-sample group
#. **Fine frequency and phase adjustment**: Apply Costas loop to lock onto the correct phase and remove residual frequency offset
#. **Plot data**: Visualize constellation diagrams and waveforms to observe signal quality

**Key Signal Processing Techniques:**

* **Frequency Offset Correction**: The received signal is squared (raising phase errors to become visible as frequency offsets), an FFT identifies the peak at 2× the offset frequency, and samples are corrected by multiplying by :math:`e^{-j2\pi f_{offset} t}`

* **Symbol Timing Recovery (Mueller and Muller)**: Since each bit is transmitted as 16 samples, the receiver must identify which of the 16 samples represents the optimal sampling point. The Mueller and Muller algorithm iteratively adjusts the timing offset to lock onto the correct samples.

* **Costas Loop (Fine Synchronization)**: Functions like a phase-locked loop (PLL) that multiplies I × Q samples. For ideal BPSK where Q = 0, this error function is minimized when there is no phase or frequency offset, allowing the loop to lock onto the correct carrier phase.

**What You Will Observe:**

Starting from a noisy, offset constellation with scattered points, you will see the signal progressively improve:

#. **Raw received signal**: Shows significant phase and frequency offset
#. **After coarse frequency correction**: Frequency offset mostly removed, but phase offset remains
#. **After symbol timing recovery**: Correct samples selected, reducing mid-constellation points
#. **After Costas loop**: Clean constellation with two tight clusters at -1 and +1

   .. figure:: images/exercises/bpsk_loopback/bpsk_constellation_progression.png
      :alt: BPSK Signal Recovery Progression
      :align: center
      :width: 40em

      Progression from noisy received signal to clean demodulated constellation

This exercise demonstrates the power of software-defined radio: complex signal processing algorithms running in software can correct real-world impairments that would be difficult or impossible to handle in analog hardware alone.

The following subsections demonstrate different implementations of BPSK using various platforms.

Using PyADI-IIO with Pluto
^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps to run the BPSK transmission and reception experiment:

#. Make the setup using Pluto with loopback connection and connect it to your PC.

#. Open the **workshop_exercises** folder from your desktop.

#. Go to **python->bpsk_loopback** subfolder.

#. Right click on **bpsk_pluto_loopback_pluto.py -> Open With Other Application** and select **Thonny Python IDE**
   (or any other Python IDE you have installed) from the Recommended Applications list.

#. To run the script, press the green round button from the top left corner in Thonny IDE.

#. To zoom on a plot, you can use the zoom option. Encircle (holding left click) the area you want to zoom.

#. To stop running the Python script (if you want to run it again or run another script), close all the tabs by **right clicking on the
   taskbar icon -> Quit x windows** or close them manually one by one.


Using PyADI-IIO with Jupiter
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This demonstrates **platform portability**—running the same BPSK algorithm on higher-performance hardware with minimal code changes.

Follow these steps:

#. From **workshop_exercises->python->bpsk_loopback** subfolder, open **bpsk_jupiter_loopback_jupiter.py** using **Thonny Python IDE**.

#. Observe in the script that only the "Configure SDR" code section changed compared to **bpsk_pluto_loopback_pluto.py**:

   .. code-block:: python
      :linenos:
      :lineno-start: 4
      :class: code-spaced

      # Configure properties
      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio

      center_freq = 915000000  # Hz
      sample_rate = 30720000  # TX sample rate
      tx_buffer_size = 1024  # TX buffer_size

      sdr.rx_ensm_mode_chan0 = "rf_enabled"
      sdr.rx_ensm_mode_chan1 = "rf_enabled"
      sdr.tx_hardwaregain_chan0 = -20
      sdr.tx_hardwaregain_chan1 = 0
      sdr.rx_ensm_mode_chan0 = "rf_enabled"
      sdr.tx_cyclic_buffer = True
      sdr.tx_buffer_size = 1024  # number of samples per call to tx()
      sdr.tx0_lo = center_freq
      sdr.tx0_lo = center_freq
      sdr.rx0_lo = center_freq

      fs = int(sdr.rx0_sample_rate)

#. Insert the IP address on line 5 to remotely connect to Jupiter:

   .. code-block:: python
      :linenos:
      :lineno-start: 5
      :class: code-spaced

      sdr = adi.adrv9002(uri="ip:10.48.65.212")  # Create Radio object for Jupiter

#. To run the script, press the green round button from the top left corner in Thonny IDE.

#. To zoom on a plot, you can use the zoom option. Encircle (holding left click) the area you want to zoom.

#. To stop running the Python script (if you want to run it again or run another script), close all the tabs by **right clicking on the
   taskbar icon -> Quit x windows** or close them manually one by one.


Using PyADI-IIO with Talise
^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Follow these steps:

#. From **workshop_exercises->python->bpsk_loopback** subfolder, open **bpsk_talise_loopback_talise_zu11eg.py** using **Thonny Python IDE**.

#. Observe the "Configure SDR" section for Talise:

   .. code-block:: python
      :linenos:
      :lineno-start: 1
      :class: code-spaced

      # Configure properties
      sdr = adi.adrv9009_zu11eg("ip:10.48.65.182")  # Create Radio
      sdr.rx_enabled_channels = [0]
      sdr.tx_enabled_channels = [0]
      sdr.tx_cyclic_buffer = True
      sdr.tx_cyclic_buffer = True
      sdr.gain_control_mode_chan0 = "slow_attack"
      sdr.gain_control_mode_chan1 = "slow_attack"
      sdr.tx_hardwaregain_chan0 = -20
      sdr.tx_hardwaregain_chan1 = -20
      sdr.tx_hardwaregain_chan1_chip_b = -80
      sdr.tx_hardwaregain_chan1_chip_b = -80
      sdr.gain_control_mode_chan0 = "slow_attack"
      sdr.gain_control_mode_chan1 = "slow_attack"
      sdr.gain_control_mode_chan0_chip_b = "slow_attack"
      sdr.gain_control_mode_chan1_chip_b = "slow_attack"
      sdr.rx_buffer_size = 32768
      sdr.tx_buffer_size = 1024

      fs = int(sdr.rx_sample_rate)

#. Insert the IP address on line 2 to remotely connect to Talise:

   .. code-block:: python
      :linenos:
      :lineno-start: 2
      :class: code-spaced

      sdr = adi.adrv9009_zu11eg("ip:10.48.65.182")  # Create Radio object for Talise

#. To run the script, press the green round button from the top left corner in Thonny IDE.

#. To zoom on a plot, you can use the zoom option. Encircle (holding left click) the area you want to zoom.

#. To stop running the Python script (if you want to run it again or run another script), close all the tabs by **right clicking on the
   taskbar icon -> Quit x windows** or close them manually one by one.


Quadrature Phase Shift Keying (QPSK)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

This exercise demonstrates Quadrature Phase Shift Keying (QPSK), a digital modulation scheme that transmits 2 bits per symbol instead of BPSK's 1 bit per symbol. QPSK uses four phase positions in the I/Q plane to encode data, effectively doubling the data rate in the same bandwidth.

**What is QPSK?**

QPSK encodes two bits into each symbol using four constellation points at 45°, 135°, 225°, and 315°:

* **00**, **01**, **10**, **11** → Four distinct phase positions

This creates twice the spectral efficiency of BPSK while maintaining good noise immunity.

   .. figure:: images/exercises/qpsk_gnuradio/qpsk_constellation.png
      :alt: QPSK Constellation
      :align: center
      :width: 40em

      QPSK constellation (4 points, 2 bits/symbol)

   .. figure:: images/exercises/qpsk_gnuradio/qpsk_ho_modulation.png
      :alt: QPSK Higher Order Modulation Constellation
      :align: center
      :width: 40em

      QPSK higher-order modulation schemes (16-QAM, 32-QAM, 64-QAM, 256-QAM)

**Where is QPSK Used?**

* Satellite communications (DVB-S, GPS)
* Wireless standards (CDMA, LTE, 5G)
* Cable modems (DOCSIS)
* Point-to-point microwave links

The following subsections demonstrate QPSK implementations ranging from basic reception to progressively more sophisticated receivers with full synchronization chains, showcasing how each processing block improves signal quality.

Using GNU Radio with Pluto - Basic Receiver
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

You will receive QPSK-modulated data packets containing hexadecimal values using GNU Radio. The flowgraph performs symbol synchronization,
frequency locking, and constellation decoding to recover the transmitted data.

   .. figure:: images/exercises/qpsk_gnuradio/qpsk_gnuradio_flowgraph.png
      :alt: QPSK GNU Radio Flowgraph
      :align: center
      :width: 40em

      GNU Radio flowgraph for receiving QPSK-modulated data using Pluto SDR

Follow these steps to receive QPSK-modulated data:

#. Make the following setup using Pluto and connect it to your PC:

   .. figure:: images/exercises/qpsk_pluto/qpsk_pluto_setup.png
      :alt: QPSK Pluto Setup
      :align: center
      :width: 40em

      QPSK Pluto Setup

#. Open the terminal and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open **qpsk_point_to_point_rx_console.grc** from **workshop_exercises->gnuradio->qpsk_point_to_point_console**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the received message stored in "msg_var" variable as depicted below.

   .. figure:: images/exercises/qpsk_pluto/qpsk_point_to_point_rx_console.png
      :alt: QPSK Point to Point RX Console
      :align: center
      :width: 40em

      QPSK Point to Point RX Console


Using GNU Radio with Jupiter - Without Additional DSP
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise demonstrates QPSK communication without proper digital signal processing.
It shows why synchronization and filtering blocks are essential in real-world systems.

**Simplified Signal Processing:**

The transmitter configures the SDR, creates an array of bits, interpolates and remaps symbols,
shifts the spectrum, and transmits the signal. Notably absent is the matched filter for pulse shaping.

The receiver receives the signal, shifts the spectrum back to baseband, and plots the data.
It lacks the frequency correction, matched filtering, timing recovery, and phase correction that
would normally be present in a proper QPSK receiver.

**What This Demonstrates:**

This exercise reveals several critical requirements for digital communication systems by intentionally omitting them.
Without proper DSP blocks, you'll observe:

* **Inefficient spectrum** - Square wave pattern wastes bandwidth and causes adjacent channel interference
* **Constellation rotation** - Phase offset causes symbols to drift from ideal positions
* **Poor timing** - Wrong samples selected from each symbol period
* **High sidelobes** - Absence of pulse shaping creates interference

These problems highlight why standard QPSK implementations require:

* **Frequency and phase correction** using Costas loops and PLLs (offsets vary with distance, temperature, and time)
* **Symbol timing recovery** algorithms like Mueller and Muller to extract correct sample timing
* **Matched filters** with pulse shaping for spectral efficiency
* **Differential encoding or preambles** to resolve constellation ambiguity

The subsequent exercises add these blocks one by one, showing how each improves the system.

Follow these steps to run this demonstration:

#. Make the hardware setup using Jupiter SDR with RX1 and TX1 connected via SMA loopback cable
   and connect it to your PC.

   .. TODO: Add Jupiter loopback setup image when available
   .. .. figure:: images/exercises/qpsk_raw/jupiter_loopback_setup.png
   ..    :alt: Jupiter SDR Loopback Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with RX1 and TX1 connected via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion
   with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open the following file from **File → Open**:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ /home/analog/Desktop/ftc_2024/10_qpsk_raw_loopback_gnuradio/QPSK_raw_loopback_jupiter.grc

#. Observe the simplified flowgraph showing transmitter and receiver without DSP correction blocks.

   .. TODO: Add raw QPSK flowgraph image when available
   .. .. figure:: images/exercises/qpsk_raw/qpsk_raw_flowgraph.png
   ..    :alt: Simplified QPSK Flowgraph Without DSP
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Simplified QPSK flowgraph without synchronization and filtering blocks

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays. The transmitted spectrum shows wide square wave characteristics
   with high sidelobes. The received constellation displays scattered points with phase offset
   instead of clean clusters at the ideal symbol positions. The received spectrum demonstrates
   wide bandwidth due to the absence of pulse shaping.

   .. TODO: Add raw QPSK output comparison image when available
   .. .. figure:: images/exercises/qpsk_raw/qpsk_raw_output_comparison.png
   ..    :alt: QPSK Without DSP Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing inefficient spectrum and constellation rotation without DSP

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter - Constellation Modulator
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise demonstrates QPSK communication with proper pulse shaping using matched Root-Raised Cosine filters.
It builds upon the previous raw QPSK exercise by adding matched filters at both transmitter and receiver,
significantly improving spectral efficiency.

**Signal Processing with Matched Filters:**

The transmitter configures the SDR, creates an array of bits, and uses a Constellation Modulator block
that maps bytes into symbols and applies a Root-Raised Cosine filter for pulse shaping. The signal spectrum
is then shifted and transmitted. The addition of matched filters at the transmitter optimizes bandwidth usage
compared to the raw square wave spectrum observed in the previous exercise.

The receiver receives the signal, applies a matched Root-Raised Cosine filter to maximize signal-to-noise ratio,
shifts the spectrum back to baseband, and plots the data. However, this implementation still lacks frequency offset correction,
symbol timing recovery, and phase correction, demonstrating the progressive improvement in QPSK receivers.

**Key Improvements Over Raw QPSK:**

The most significant improvement is spectral efficiency. The Root-Raised Cosine matched filters at transmitter and receiver
eliminate the high sidelobes seen in the raw QPSK implementation, concentrating signal energy within the necessary bandwidth.
This reduces adjacent channel interference and allows more efficient spectrum usage.

The received signal quality improves due to the matched filter maximizing signal-to-noise ratio by correlating
with the expected pulse shape. However, without symbol timing recovery, the receiver does not yet decimate
by selecting the optimal samples from each symbol period.

**What is Root-Raised Cosine (RRC) Filtering?**

Root-Raised Cosine filtering is a pulse shaping technique that satisfies the Nyquist criterion for zero
intersymbol interference. By using matched RRC filters at both transmitter and receiver, the combined response
forms a complete raised cosine filter that eliminates interference between adjacent symbols while minimizing bandwidth.

**What You'll See in This Exercise:**

When you run this demonstration, observe how the Constellation Modulator maps bits into IQ symbols and applies
RRC filtering, optimizing bandwidth usage compared to the raw square wave spectrum from the previous exercise.
The transmitted spectrum will show significantly improved bandwidth efficiency with minimal sidelobes. The matched
RRC filter at the receiver maximizes signal-to-noise ratio, though the implementation still lacks frequency offset
correction, symbol timing recovery, and phase correction—improvements that come in subsequent exercises.

Follow these steps to run this demonstration:

#. Make the hardware setup using Jupiter SDR with RX1 and TX1 connected via SMA loopback cable
   and connect it to your PC.

   .. TODO: Add Jupiter loopback setup image when available
   .. .. figure:: images/exercises/qpsk_constellation_mod/jupiter_loopback_setup.png
   ..    :alt: Jupiter SDR Loopback Setup
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with RX1 and TX1 connected via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion
   with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open the following file from **File → Open**:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ /home/analog/Desktop/ftc_2024/12_constellation_modulator_loopback_gnuradio/constellation_modulator_loopback_jupiter.grc

#. Observe the flowgraph structure showing the Constellation Modulator block and matched RRC filters.

   The Constellation Modulator block in the transmitter maps bytes into symbols and applies the RRC filter
   in a single operation. The receiver includes a separate RRC filter block to complete the matched filter pair.

   .. TODO: Add Constellation Modulator flowgraph image when available
   .. .. figure:: images/exercises/qpsk_constellation_mod/constellation_mod_flowgraph.png
   ..    :alt: QPSK Constellation Modulator Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    QPSK flowgraph with Constellation Modulator and matched RRC filters

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays and compare to the previous raw QPSK exercise.

   The transmitted spectrum shows significantly improved bandwidth efficiency with minimal sidelobes due to
   RRC pulse shaping. The received signal demonstrates better quality from the matched filter processing.
   However, the received signal is not yet decimated by selecting the optimal samples from each symbol period,
   which would be added in a complete receiver implementation.

   .. TODO: Add Constellation Modulator output image when available
   .. .. figure:: images/exercises/qpsk_constellation_mod/constellation_mod_output.png
   ..    :alt: QPSK Constellation Modulator Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing optimized bandwidth usage with matched RRC filters

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Jupiter - Frequency Locked Loop
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise builds upon the previous Constellation Modulator demonstration by adding coarse frequency offset
correction using a Frequency Locked Loop. This represents the next step in building a complete QPSK receiver
by addressing frequency differences between transmitter and receiver oscillators.

**Signal Processing with Frequency Correction:**

The transmitter chain remains the same as the previous exercise, using the Constellation Modulator to map bits
into symbols with Root-Raised Cosine pulse shaping, shifting the spectrum, and transmitting the signal.

The receiver now includes a critical addition to the signal processing chain. After receiving the signal through
the matched RRC filter, a Frequency Locked Loop block dynamically adjusts the frequency of the received spectrum
to compensate for oscillator differences between transmitter and receiver. The receiver then shifts the spectrum
back to baseband and plots the data. However, fine frequency and phase adjustment using a Costas loop and symbol
timing recovery for optimal sample selection are still missing from this implementation.

**What is a Frequency Locked Loop?**

A Frequency Locked Loop is a control system that automatically adjusts for coarse frequency offsets between
transmitter and receiver local oscillators. Unlike a Phase Locked Loop which tracks both frequency and phase,
an FLL focuses specifically on correcting frequency errors, making it useful as a first stage before fine
synchronization with a PLL or Costas loop.

The FLL block continuously monitors the received spectrum and adjusts its center frequency to compensate for
drift, ensuring the signal remains centered around DC (zero frequency) for subsequent processing stages.

**Key Improvements Over Constellation Modulator:**

The most significant improvement is automatic frequency offset correction. Without this correction, frequency
differences between TX and RX oscillators would cause the constellation to rotate continuously, making reliable
demodulation impossible. The FLL ensures the spectrum stays centered around DC, enabling subsequent processing stages.

However, the receiver still lacks fine frequency and phase correction that a Costas loop would provide, and symbol
timing recovery to select the optimal samples from each symbol period. These will be added in more complete implementations.

**What You'll Observe:**

When running this exercise, watch how the FLL automatically adjusts for frequency differences between transmitter
and receiver oscillators. The transmitted spectrum shows optimized bandwidth from RRC filtering. Even as you adjust
the frequency offset slider, the spectrum after the FLL block stays centered around DC, demonstrating automatic
frequency tracking. The constellation shows improved stability compared to implementations without frequency correction,
though fine frequency/phase correction (Costas loop) and symbol timing recovery would further improve quality.

Follow these steps to run this demonstration:

#. Make the hardware setup using Jupiter SDR with RX1 and TX1 connected via SMA loopback cable
   and connect it to your PC.

   .. TODO: Add Jupiter loopback setup image when available
   .. .. figure:: images/exercises/qpsk_fll/jupiter_loopback_setup.png
   ..    :alt: Jupiter SDR Loopback Setup for FLL
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with RX1 and TX1 connected via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion
   with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open the following file from **File → Open**:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ /home/analog/Desktop/ftc_2024/13_fll_loopback_gnuradio/FLL_loopback_jupiter.grc

#. Observe the flowgraph structure showing the FLL Band-Edge block for frequency correction.

   The transmitter section includes the Constellation Modulator with RRC filtering. The receiver section
   now features the FLL Band-Edge filter block highlighted in the flowgraph, which dynamically adjusts
   the frequency of the received spectrum to keep it centered around DC.

   .. TODO: Add FLL flowgraph image when available
   .. .. figure:: images/exercises/qpsk_fll/fll_flowgraph.png
   ..    :alt: QPSK FLL Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    QPSK flowgraph with Frequency Locked Loop for coarse frequency correction

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays and use the frequency offset slider to adjust the offset between TX and RX.

   The transmitted spectrum shows the optimized bandwidth from RRC filtering. The received spectrum after
   the FLL block stays centered around DC even as you adjust the frequency offset slider, demonstrating
   the FLL's ability to track and correct frequency errors. The received constellation shows improved
   stability compared to implementations without frequency correction.

   Note that while the FLL keeps the spectrum centered, a more precise frequency offset correction using
   a Costas loop would further improve constellation quality. Additionally, the receiver does not yet
   perform symbol timing recovery to select optimal samples.

   .. TODO: Add FLL output image when available
   .. .. figure:: images/exercises/qpsk_fll/fll_output.png
   ..    :alt: QPSK FLL Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Output showing FLL maintaining spectrum centered around DC despite frequency offset

#. Use the offset slider to introduce frequency offset and observe how the spectrum after the FLL block
   automatically adjusts to stay centered around DC.

#. Click the **Stop** button (red square) when finished.


Using GNU Radio with Pluto - Packet-Based File Storage
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise builds upon the previous QPSK demonstrations by implementing a complete **packet-based communication system**. Instead of displaying the received message on the console, this system receives QPSK-modulated data packets transmitted at regular intervals and extracts the message content to a file, demonstrating how real-world communication systems handle burst transmissions.

**How Packet-Based Communication Works:**

Unlike the continuous QPSK transmission you saw in previous exercises, this system transmits data in **bursts** (packets) at specific time intervals. Each packet contains:

* **Access Key (Preamble)**: A known synchronization pattern that helps the receiver detect the start of a packet
* **Payload Length**: Information about how much data is in the packet
* **Message Data**: The actual content being transmitted (e.g., "ANALOG DEVICES INC FTC2024")
* **CRC32 Checksum**: Error detection code to verify data integrity

**Why Packet-Based Transmission?**

Most real-world communication systems use packet-based transmission because:

* **Efficiency**: Only transmit when you have data to send, saving power and bandwidth
* **Multiple Access**: Allows multiple users to share the same frequency by taking turns
* **Error Detection**: CRC32 checksum catches transmission errors
* **Synchronization**: The receiver can lock onto each burst independently

**GNU Radio Signal Flow:**

**Transmitter Chain:**

#. **Message Source**: Sends specific messages at regular time intervals
#. **Packet Formatter**: Appends access key and payload length to each message
#. **CRC32**: Appends checksum for error detection
#. **PDU to Tagged Stream**: Converts packets (Protocol Data Units) to a continuous stream of data tagged with metadata
#. **QPSK Modulator**: Modulates the data using Quadrature Phase Shift Keying
#. **SDR Sink**: Transmits the modulated signal through the antenna

**Receiver Chain:**

#. **SDR Source**: Receives the RF signal from the antenna
#. **QPSK Demodulator**: Demodulates the signal to recover the symbol stream (similar to the previous exercise with frequency/phase/timing recovery)
#. **Packet Extractor**: Extracts the message from the received stream of bits by detecting the access key, reading payload length, extracting data bytes, and verifying CRC32
#. **File Sink**: Stores the decoded message to a file instead of displaying on console

**Key Observation - Burst Mode Effects:**

When you observe the constellation diagram for this exercise, you'll notice that **the symbols appear noisier** compared to the continuous QPSK transmission. This is because:

* Data is sent in **bursts** rather than continuously
* The receiver must re-synchronize for each packet
* Between bursts, there's no signal, creating transitions in the constellation
* The Costas loop and timing recovery have less time to stabilize for each burst

This is normal behavior for packet-based systems and demonstrates the trade-off between efficiency (burst mode) and signal quality (continuous mode).

Follow these steps to receive and store QPSK-modulated messages:

#. Make the hardware setup using Jupiter SDR and connect it to your PC.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/qpsk_file/jupiter_setup.png
   ..    :alt: Jupiter SDR Hardware Setup for QPSK Message Reception
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup for receiving QPSK modulated messages

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **qpsk_point_to_point_rx_file.grc** from **workshop_exercises->gnuradio->qpsk_point_to_point_file**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

   .. TODO: Add flowgraph diagram when available
   .. .. figure:: images/exercises/qpsk_file/qpsk_flowgraph.png
   ..    :alt: QPSK Packet Communication Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    GNU Radio flowgraph showing transmitter and receiver chains for packet-based QPSK communication

#. Observe the received message and signal quality in the GUI displays:

   * **Received Samples (After Costas Loop)**: Time-domain I/Q waveform
   * **Rx Re and Rx Im (After Costas Loop)**: Zoomed time-domain view of in-phase and quadrature components
   * **Received Spectrum**: Frequency-domain representation showing the QPSK signal bandwidth
   * **Received Constellation**: QPSK constellation points (note the increased noise due to burst transmission)
   * **msg_rx variable**: Displays the decoded message (e.g., "ANALOG DEVICES INC FTC2024")

   .. TODO: Add output screenshot when available
   .. .. figure:: images/exercises/qpsk_file/qpsk_rx_output.png
   ..    :alt: QPSK Message Reception Output
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Received QPSK message and signal analysis plots

#. The decoded message is automatically saved to a file in the project directory for later analysis.

#. Click the **Stop** button (red square) when finished.


Using SDRangel with Pluto - DATV Video Reception
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

This exercise demonstrates a real-world application of QPSK modulation: receiving Digital Amateur Television (DATV). Using the open-source `SDRangel <https://www.sdrangel.org/>`_ software, you will receive and display live video transmitted over the air using QPSK modulation, the same technique used in satellite TV broadcasting.

**What is DATV?**

Digital Amateur Television (DATV) is a form of amateur radio that transmits video and audio using digital modulation schemes like QPSK. Unlike analog TV, DATV provides:

* Better quality with less interference
* More efficient use of bandwidth
* Error correction capabilities
* Multiple audio/video streams in a single channel

**How This Exercise Works:**

`SDRangel <https://www.sdrangel.org/>`_ is a powerful open-source SDR application that implements a complete DATV receiver chain including QPSK demodulation, Forward Error Correction (FEC), and MPEG-2/H.264 video decoding. The Pluto SDR receives the RF signal, and SDRangel processes it to display the video in real-time.

Follow these steps to receive QPSK-modulated video:

#. Make the setup using Pluto and connect it to your PC:

   .. figure:: images/exercises/qpsk_pluto/qpsk_pluto_setup.png
      :alt: QPSK Pluto Setup
      :align: center
      :width: 40em

      QPSK Pluto Setup

#. Open the terminal by pressing at the same time **Ctrl + Alt + T** on your keyboard.

#. Type the following command in the terminal and press Enter:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ sdrangel

#. The application should be preconfigured. Run the program by pressing the purple arrow as depicted below.

   .. figure:: images/exercises/qpsk_video/sdrangel_run.png
      :alt: Run SDRangel
      :align: center
      :width: 40em

      Run SDRangel to receive QPSK modulated video

#. On DATV window, **check and then uncheck Viterbi** to synchronize the qpsk data **while the program is running** (as shown below).

   .. figure:: images/exercises/qpsk_video/viterbi_sync.png
      :alt: Viterbi Sync
      :align: center
      :width: 40em

      Check and then uncheck Viterbi to synchronize the QPSK data in SDRangel

Spectrum Paint using Pluto
~~~~~~~~~~~~~~~~~~~~~~~~~~

This exercise demonstrates a creative application of SDR: "painting" images in the frequency spectrum. By transmitting signals at different frequencies over time, you can create visible patterns on a waterfall display, which shows frequency on the horizontal axis and time on the vertical axis.

**How Spectrum Paint Works:**

A waterfall display is a spectrogram that shows:

* **Frequency** (horizontal axis): Different colors represent signal strength at each frequency
* **Time** (vertical axis): Each new row represents a new time snapshot
* **Amplitude** (color intensity): Brighter colors indicate stronger signals

By carefully controlling which frequencies are transmitted at each moment, you can "draw" images, text, or patterns that become visible as the waterfall scrolls down.

**Applications:**

While spectrum paint is primarily a demonstration, the underlying technique is used in:

* Frequency-hopping spread spectrum (FHSS)
* Chirp radar systems
* Multi-carrier communications
* Signal identification and watermarking

This exercise showcases the flexibility of software-defined radio: the same hardware can transmit any arbitrary frequency pattern by simply changing the software.

Follow these steps to create a spectrum painting:

#. Make the following setup using Pluto and connect it to your PC.

#. Open the terminal and launch GNU Radio Companion:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ gnuradio-companion

#. In GNU Radio Companion, open the **paint_tx.grc** file from **workshop_exercises->gnuradio->spectrum_paint**.

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the waterfall display as the spectrum painting appears, created by the Pluto SDR transmitting the frequency pattern.

   .. figure:: images/exercises/spectrum_paint/paint_tx.png
      :alt: Spectrum Paint Waterfall Display
      :align: center
      :width: 40em

      Waterfall display showing the spectrum painting created by transmitting controlled frequency patterns

Amplitude Shift Keying (ASK) in GNU Radio using Jupiter
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

This exercise demonstrates Amplitude Shift Keying (ASK), a digital modulation technique where information is encoded by varying the amplitude of a carrier signal. Unlike BPSK which varies the phase, or FSK which varies the frequency, ASK varies the signal strength to represent different symbols.

**What is ASK?**

Amplitude Shift Keying is a form of amplitude modulation where digital data is represented by changes in the amplitude of a carrier wave:

* **Different amplitude levels** represent different symbols
* In this exercise, we use **4-ASK** (4 amplitude levels): 0, 1/3, 2/3, and 1
* Each symbol represents 2 bits of data (00, 01, 10, 11)
* The carrier phase remains constant while amplitude varies

**Where is ASK Used?**

ASK and its variants are used in various communication systems:

* **RFID tags** - Simple, low-power devices
* **Optical fiber communications** - On-Off Keying (OOK) is a binary form of ASK
* **Infrared remote controls** - Simple amplitude modulation
* **Combined with PSK** - Forms QAM (Quadrature Amplitude Modulation) used in Wi-Fi, LTE, 5G, cable modems

**How This Exercise Works:**

This GNU Radio flowgraph demonstrates 4-level ASK transmission and reception with the following signal processing:

**Transmitter Chain:**

#. **Vector Source**: Generates four symbols repeatedly: [0, 1/3, 2/3, 1]
#. **Repeat Block**: Repeats each symbol 100 times (100 samples per symbol) to create a visible waveform
#. **Multiply by Complex Sinusoid**: Offsets the frequency of the transmitted signal relative to the TX LO frequency
#. **SDR Sink (Jupiter)**: Transmits the amplitude-modulated signal

**Receiver Chain:**

#. **SDR Source (Jupiter)**: Receives the RF signal from the loopback connection
#. **Complex to Magnitude**: Extracts only the amplitude information from the received I/Q signal (discards phase)
#. **Keep 1 in N (Decimation)**: Keeps only 1 sample out of every 100 (recovers the original symbol rate)
#. **GUI Displays**: Shows constellation, spectrum, time-domain waveforms, and decimated output

**Key Observations:**

* **Constellation Diagram**: You will observe **4 distinct amplitude levels** on the real axis (0, 1/3, 2/3, 1)
* **Magnitude RX Plot**: Shows the varying amplitude levels in the time domain
* **Decimated RX**: After decimation, you see the original symbol sequence recovered
* **Phase Information Discarded**: Unlike BPSK/QPSK, only the magnitude matters in ASK, making it simpler but more susceptible to noise

**From ASK to QAM - The Next Step:**

When you combine **Amplitude Shift Keying (ASK)** with **Phase Shift Keying (PSK)**, you get **Quadrature Amplitude Modulation (QAM)**. QAM is one of the most widely used modulation schemes in modern communications because it achieves high data rates by encoding information in both amplitude and phase:

* **16-QAM**: 4 amplitude levels × 4 phase positions = 16 symbols (4 bits per symbol)
* **32-QAM**: 32 symbols (5 bits per symbol)
* **64-QAM**: 64 symbols (6 bits per symbol) - used in Wi-Fi 802.11n/ac
* **256-QAM**: 256 symbols (8 bits per symbol) - used in Wi-Fi 6, cable modems, LTE Advanced

   .. TODO: Add QAM constellation diagrams image when available
   .. .. figure:: images/exercises/ask_gnuradio/qam_constellations.png
   ..    :alt: QAM Constellation Diagrams
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Higher-order QAM constellations combining amplitude and phase modulation

Follow these steps to run the ASK transmission and reception experiment:

#. Make the hardware setup using Jupiter SDR with TX1A connected to RX1A via loopback cable and connect it to your PC.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/ask_gnuradio/jupiter_setup.png
   ..    :alt: Jupiter SDR Hardware Setup for ASK
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with TX1A connected to RX1A via loopback cable

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **ASK_loopback_jupiter.grc** from **Desktop->ftc_2024->8_ask_loopback_gnuradio**.

#. Observe the flowgraph structure showing the transmitter and receiver chains:

   .. TODO: Add flowgraph diagram when available
   .. .. figure:: images/exercises/ask_gnuradio/ask_flowgraph.png
   ..    :alt: ASK Loopback Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    GNU Radio flowgraph showing ASK transmitter and receiver signal processing

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays:

   * **Transmitted Samples**: Shows the repeated symbols with 100 samples per symbol
   * **Received Samples (Unprocessed)**: Raw I/Q signal from the receiver showing amplitude variations
   * **Received Spectrum**: Frequency-domain representation showing the offset carrier
   * **Received Constellation**: Shows **4 distinct amplitude levels** on the real axis (0, 1/3, 2/3, 1)
   * **Magnitude RX**: Time-domain plot showing only the amplitude of the received signal
   * **Decimated RX**: After keeping 1 in 100 samples, showing the recovered symbol sequence

   .. TODO: Add output screenshot when available
   .. .. figure:: images/exercises/ask_gnuradio/ask_output.png
   ..    :alt: ASK Output Plots
   ..    :align: center
   ..    :width: 40em
   ..
   ..    ASK signal processing output showing transmitted symbols, received constellation, and magnitude plots

#. You can adjust the frequency offset slider to observe how the received signal changes in the spectrum display.

#. Click the **Stop** button (red square) when finished.


Frequency Shift Keying (FSK) in GNU Radio using Jupiter
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

This exercise demonstrates Frequency Shift Keying (FSK), a digital modulation technique where information is encoded by varying the frequency of a carrier signal. Unlike BPSK which varies the phase, or ASK which varies the amplitude, FSK switches between different frequencies to represent different symbols.

**What is FSK?**

Frequency Shift Keying is a form of frequency modulation where digital data is represented by discrete frequency shifts of a carrier wave:

* **Different frequencies** represent different symbols
* In this exercise, we use **4-FSK** (4 distinct frequencies)
* Each symbol represents 2 bits of data (00, 01, 10, 11)
* The amplitude and phase remain relatively constant while frequency changes

**Where is FSK Used?**

FSK and its variants are widely used in communication systems:

* **Caller ID** - Phone systems use FSK to transmit caller information
* **Radio Telemetry** - Weather stations, remote sensors
* **Pagers** - POCSAG and FLEX paging protocols use FSK
* **Bluetooth** - Uses Gaussian FSK (GFSK) for robust communication
* **LoRa** - Long-range IoT uses Chirp Spread Spectrum (CSS), a continuous form of FSK
* **Older modems** - Bell 103 and 202 modems used FSK at 300-1200 baud
* **APRS** - Automatic Packet Reporting System for amateur radio

**Advantages of FSK:**

* **Noise resilience** - Frequency changes are less affected by amplitude noise
* **Constant envelope** - Power remains constant, efficient for power amplifiers
* **Simple demodulation** - Can be demodulated with simple discriminators
* **Works well in fading channels** - Better performance than ASK in multipath environments

**How This Exercise Works:**

This GNU Radio flowgraph demonstrates 4-level FSK transmission and reception with the following signal processing:

**Transmitter Chain:**

#. **Vector Source**: Generates four symbols repeatedly representing different frequencies
#. **Repeat Block**: Repeats each symbol to create longer frequency bursts
#. **VCO (Voltage Controlled Oscillator)**: Converts the symbol values to corresponding frequency shifts
#. **Multiply Const**: Adjusts the frequency deviation (how far apart the frequencies are)
#. **SDR Sink (Jupiter)**: Transmits the frequency-modulated signal

**Receiver Chain:**

#. **SDR Source (Jupiter)**: Receives the RF signal from the loopback connection
#. **Quadrature Demod**: Converts frequency changes to amplitude changes (FM demodulation)
#. **Keep 1 in N (Decimation)**: Recovers the original symbol rate
#. **GUI Displays**: Shows spectrum with multiple frequency peaks, time-domain waveforms, and the demodulated output

**Key Observations:**

* **Frequency Spectrum**: You will observe **4 distinct frequency peaks** corresponding to the four FSK tones
* **Time Domain**: The received signal shows frequency transitions between symbols
* **Waterfall Display**: Shows the frequency hopping pattern over time
* **Demodulated Output**: After quadrature demodulation, you see the original symbol sequence recovered

**From FSK to More Advanced Modulations:**

FSK represents one dimension of modulation (frequency). Modern communication systems combine multiple dimensions:

* **FSK + Time**: Frequency-hopping spread spectrum (FHSS) used in Bluetooth
* **Continuous FSK**: Gaussian FSK (GFSK) smooths transitions to reduce bandwidth
* **Chirp Spread Spectrum (CSS)**: Continuously sweeps frequency, used in LoRa for long-range IoT
* **OFDM**: Uses many closely-spaced FSK-like subcarriers, foundation of Wi-Fi and LTE

   .. TODO: Add FSK theory diagram when available
   .. .. figure:: images/exercises/fsk_gnuradio/fsk_theory.png
   ..    :alt: FSK Modulation Theory
   ..    :align: center
   ..    :width: 40em
   ..
   ..    FSK modulation showing four different frequencies representing different symbols

Follow these steps to run the FSK transmission and reception experiment:

#. Make the hardware setup using Jupiter SDR with TX1A connected to RX1A via loopback cable and connect it to your PC.

   .. TODO: Add Jupiter hardware setup image when available
   .. .. figure:: images/exercises/fsk_gnuradio/jupiter_fsk_setup.png
   ..    :alt: Jupiter SDR Hardware Setup for FSK
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Jupiter SDR setup with TX1A connected to RX1A via loopback cable for FSK experiment

#. Open the terminal and navigate to the Desktop directory, then launch GNU Radio Companion with elevated privileges:

   .. shell::
      :user: analog
      :group: analog
      :show-user:

      $ cd /home/analog/Desktop
      $ sudo ./start-grc.sh

#. In GNU Radio Companion, open **FSK_loopback_jupiter.grc** from **Desktop->ftc_2024->9_fsk_loopback_gnuradio**.

#. Observe the flowgraph structure showing the transmitter and receiver chains:

   .. TODO: Add flowgraph diagram when available
   .. .. figure:: images/exercises/fsk_gnuradio/fsk_flowgraph.png
   ..    :alt: FSK Loopback Flowgraph
   ..    :align: center
   ..    :width: 40em
   ..
   ..    GNU Radio flowgraph showing FSK transmitter with VCO and receiver with quadrature demodulator

#. Click the **Execute** button (green arrow) to run the flowgraph.

#. Observe the output displays:

   * **Transmitted Symbols**: Shows the repeated symbol values that control the VCO
   * **Transmitted Spectrum**: Frequency-domain showing **4 distinct frequency peaks** for the 4 FSK tones
   * **Waterfall Display**: Time-frequency representation showing frequency hopping pattern
   * **Received Spectrum**: Should match the transmitted spectrum showing the 4 FSK tones
   * **Received Time Domain**: I/Q waveform showing frequency transitions
   * **Demodulated Output**: After quadrature demod and decimation, showing the recovered symbol sequence

   .. TODO: Add output screenshot when available
   .. .. figure:: images/exercises/fsk_gnuradio/fsk_spectrum_output.png
   ..    :alt: FSK Spectrum with Multiple Frequency Peaks
   ..    :align: center
   ..    :width: 40em
   ..
   ..    FSK signal spectrum showing four distinct frequency peaks corresponding to the four transmitted tones

#. You can adjust the frequency deviation slider (if available) to observe how the spacing between frequency peaks changes.

#. Observe the waterfall display to see the frequency transitions over time:

   .. TODO: Add waterfall screenshot when available
   .. .. figure:: images/exercises/fsk_gnuradio/fsk_waterfall.png
   ..    :alt: FSK Waterfall Display
   ..    :align: center
   ..    :width: 40em
   ..
   ..    Waterfall display showing FSK frequency transitions over time as the transmitter cycles through the four tones

#. Click the **Stop** button (red square) when finished.
