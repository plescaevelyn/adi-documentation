:doc:`Click here to return to A2B Analyzer Studio Homepage. </wiki-migration/resources/tools-software/a2b-analyzer-studio>`

:doc:`Click here to return to A2B Bus Analyzer Homepage. </wiki-migration/resources/tools-software/a2b-bus-analyzer>`

A2B Analyzer Studio & A2B Bus Analyzer Frequently Asked Questions
=================================================================

The following page contains a list of Frequently Asked Questions about A\ :sup:`2`\ B Bus Analyzer and A\ :sup:`2`\ B Analyzer Studio.

FAQs on General Information
---------------------------

\*\* 1. What is the A2B Bus Analyzer Software and hardware? \*\*

A:  Analog Devices' A\ :sup:`2`\ B Bus Analyzer software is a robust development tool supporting the design and debug of AD242x/3x (except AD2437) based A\ :sup:`2`\ B networks using the Analog Devices' A\ :sup:`2`\ B Bus Analyzer hardware . The A\ :sup:`2`\ B Bus Analyzer can be configured to act as an active bus emulation tool and/or as a passive bus monitor.

\*\* 2. What is A2B Analyzer Studio? \*\*

A\ :sup:`2`\ B Analyzer Studio is the next-generation continuation of A\ :sup:`2`\ B Bus Analyzer Software, introducing new features and support for the new A\ :sup:`2`\ B Analyzer HP as well as the original A\ :sup:`2`\ B Bus Analyzer. As well as new functionality and new hardware support,  A\ :sup:`2`\ B Analyzer Studio has carried out user experience improvements based on customer interviews.

A\ :sup:`2`\ B Bus Analyzer projects can be opened with  A\ :sup:`2`\ B Analyzer Studio and they will be updated to the latest format.

The  A\ :sup:`2`\ B Bus Analyzer SDK API can be used in  A\ :sup:`2`\ B Bus Analyzer Studio but it is deprecated. We recommend that you use the API starting with a2ba2_sdk instead.

\*\* 3. Can I have both A2B Bus Analyzer and A2B Analyzer Studio Software installed at the same time? \*\*

Yes. Both the software's can be installed at the same time on a PC.

\*\* 4. What is A2B Analyzer HP? \*\*

 Analog Devices' A\ :sup:`2`\ B Bus Analyzer HP is the enhanced hardware version with Personality modules currently supporting node emulation and bus monitoring capabilities for the ADAA245x transceivers. Support for AD242x/3x based A2B networks on A\ :sup:`2`\ B Bus Analyzer HP is planned in the future.

\*\* 5. What are the modes can the A2B Bus Analyzer and A2B Analyzer HP device be used? \*\*

A: It can be used as Bus Monitor, Main Node Emulator and Sub Node Emulator.

\*\* 6. What features are supported in the A2B Bus Analyzer and A2B Analyzer HP ? \*\*

A:  The features that are supported in the A\ :sup:`2`\ B Bus Analyzer  and A\ :sup:`2`\ B Analyzer HP are: Bus monitoring, Audio recording, Event traffic monitoring, node emulation, register access, exporting register details, configuration logging.

\*\* 7. Can A2B Analyzer HP be used for for all A2B families of transceivers (A2B1.0-242x, 243x,A2B-2.0 245x etc.)? \*\*

A: A\ :sup:`2`\ B Analyzer HP currently supports the A2B2.0 transceiver family (ADAA245x) . We plan to extend support to A2B1.0 transceiver family in future. The original A\ :sup:`2`\ B Bus Analyzer hardware supports all A2B1.0 family of transceivers(AD242x and AD2435W/33W/32W/31W/30W/38W - HDAC/Duraclik connectors), except AD241x and AD2437 (XLR/RJ45 connectors)

\*\* 8. How can I order a new A2B Bus Analyzer device? \*\*

A:  Orders can be placed through the Analog Devices Website :adi:`A2B Bus Analyzer Evaluation Board \| Analog Devices <en/design-center/evaluation-hardware-and-software/evaluation-boards-kits/a2b-bus-analyzer.html#eb-buy>`

\*\* 9. How can I order a new A2B Analyzer HP device? \*\*

A:  Requests for A\ :sup:`2`\ B Analyzer HP device can be sent to a2b.analyzer.support@analog.com

\*\* 10. Where can I get latest A2B Bus Analyzer Software? \*\*

A:  The latest A\ :sup:`2`\ B Bus Analyzer and Software is available in `:doc:`/wiki-migration/resources/tools-software/a2b-bus-analyzer/software-downloads` </resources/tools-software/a2b-bus-analyzer/software-downloads>`__

\*\* 11. Where can I get latest A2B Analyzer Studio? \*\*

A:  The latest A\ :sup:`2`\ B Bus Analyzer Studio Software is available in `:doc:`/wiki-migration/resources/tools-software/a2b-analyzer-studio/software-downloads` </resources/tools-software/a2b-analyzer-studio/software-downloads>`__

\*\* 12. Can I upgrade to A2B Analyzer Studio from my A2B Bus Analyzer Software User Interface? \*\*

While A\ :sup:`2`\ B Analyzer Studio is designed to replace  A\ :sup:`2`\ B Bus Analyzer for all hardware, we made the decision to make them completely separate products. This means that  A\ :sup:`2`\ B Analyzer Studio is not an update from  A\ :sup:`2`\ B Bus Analyzer.

To install  A\ :sup:`2`\ B Analyzer Studio for the first time you must run the .exe which can be obtained from  `:doc:`/wiki-migration/resources/tools-software/a2b-analyzer-studio/software-downloads` </resources/tools-software/a2b-analyzer-studio/software-downloads>`__.

After the first installation, A\ :sup:`2`\ B Analyzer Studio supports automatic checks for updates in the same way as the previous software. The firmware on the A2B Bus Analyzer hardware must be upgraded to the latest released software from A\ :sup:`2`\ B Analyzer Studio in order to use it with that Software

\*\* 13. Which Operating Systems are supported by the A2B Bus Analyzer and A2B Analyzer Studio? \*\*

A:  The A\ :sup:`2`\ B Bus Analyzer and A\ :sup:`2`\ B  Analyzer Studio software runs on Windows, Linux, and macOS.

\*\* 14. Whom should I contact for A2B Bus Analyzer and A2B Analyzer Studio queries? \*\*

A: For any A\ :sup:`2`\ B Bus Analyzer queries, you can contact a2b.analyzer.support@analog.com

\*\* 15. Where can I get the A2B Bus Analyzer and A2B Bus Analyzer Studio Quick Start Guide? \*\*

A: The A\ :sup:`2`\ B Analyzer Studio Quick Start Guide is available in :doc:`https:wiki.analog.com/resources/tools-software/a2b-analyzer-studio/quick-start-guide]] The A\ 2\ B Bus Analyzer Quick Start Guide is available in [[/resources/tools-software/a2b-bus-analyzer/quick-start-guide|https:\ wiki.analog.com/resources/tools-software/a2b-bus-analyzer/quick-start-guide </wiki-migration/resources/tools-software/a2b-analyzer-studio/quick-start-guide>`

\*\* 16. Where can I get the A2B Bus Analyzer and A2B Bus Analyzer Studio User Guide? \*\*

A:  You can get the Software User guide by launching the appropriate tool and choose Help -> User Guide. This opens the PDF which is located in your installation.

\*\* 17. Where can I find out about known functional limitations for both A2B Bus Analyzer and A2B Bus Analyzer Studio? \*\*

A:  The functional limitations and known issues are available in `:doc:`/wiki-migration/resources/tools-software/a2b-bus-analyzer/known-issues` </resources/tools-software/a2b-bus-analyzer/known-issues>`__

\*\* 18. Do I need to follow any specific sequence for using A2B Analyzer or A2B Analyzer HP devices? \*\*

A:Refer to the Quick start guide :doc:`https:wiki.analog.com/resources/tools-software/a2b-analyzer-studio/quick-start-guide]] or [[/resources/tools-software/a2b-bus-analyzer/quick-start-guide|https:\ wiki.analog.com/resources/tools-software/a2b-bus-analyzer/quick-start-guide </wiki-migration/resources/tools-software/a2b-analyzer-studio/quick-start-guide>` to guide you through the basics of the software

\*\* 19. How can I get the latest firmware for the device? \*\*

A:  The current firmware version on the device is shown on the Home screen after connecting to an A\ :sup:`2`\ B Analyzer HP or A\ :sup:`2`\ B Bus Analyzer device. If a firmware update is available, click 'Update Firmware' and follow the on-screen instructions to flash the new version to your hardware.

\*\* 20. My UI was connected to an analyzer unit, I disconnected and switched boards and now I cannot connect unless I restart the UI. Is there a bug? \*\*

A: The A\ :sup:`2`\ B Analyzer Studio and A\ :sup:`2`\ B Bus Analyzer UI do not update the available board list when a new board is plugged in or removed. It is up to you to press refresh to generate a new list of connected boards. If you try to connect to a board that you unplugged then the UI will fail to connect to it. You should be able to distinguish the boards by their serial number which is displayed on the devices dropdown. The list is automatically generated when you start the UI which is the reason why quitting and restarting works correctly.

\*\* 21. When I am using emulator and monitor together I do not see any data in monitor even if I have already started the emulator. Why is this? \*\*

A:  Since Monitor is connected to a live network for the first time, it does not have the stream configuration stored in its memory and may not show any data.

 Recommendation for

*A2B Analyzer Studio*:

-  Select "Normal" mode while connecting to monitor in the UI
-  Press "Start Monitor" button
-  Ensure 'Data out on Bus Monitor' is enabled on Main node (i.e . ENDSNIFF bit of DATCTL register)
-  Discover the network using SS/SS+ (or A2BA as main node emulator) and verify that Monitor displays both Audio and event data on the UI.

Since Monitor has already learnt the stream configuration from previous discoveries, it restores the configuration from device memory. Recommendation: (assumption is that network is already discovered before connecting A2BA Monitor)

-  Select "Post-Discovery" mode in the monitor toolbar while connecting to Monitor in the UI.
-  Press "Start Monitor" button
-  At this point, the Monitor status indicator in the UI would turn green and events on the bus start showing up on the UI, However, it is still required to refresh the DATCTL register from main node for Monitor to be able to sniff audio data.

*A2B Bus Analyzer*:

-  Uncheck the checkbox 'discovered' while connecting to Monitor in the UI.
-  Press play button of the Monitor on the UI
-  Ensure 'Data out on Bus Monitor' is enabled on Main node (i.e . ENDSNIFF bit of DATCTL register)
-  Discover the network using SS (or A2BA as main node emulator) and verify that Monitor displays both Audio and event data on the UI.

Since Monitor has already learnt the stream configuration from previous discoveries, it restores the configuration from device memory. Recommendation: (assumption is that network is already discovered before connecting A2BA Monitor)

-  Check the checkbox 'discovered' while connecting to Monitor in the UI.
-  Press play button of the Monitor on the UI
-  At this point, all three indicators on the Monitor UI would turn green and events on the bus start showing up on the UI, However, it is still required to refresh the DATCTL register from main node for Monitor to be able to sniff audio data.

\*\* 22. If I export my event log to a .csv file and open it with Excel the timestamp is incorrect. Does the export contain different data from the event table? \*\*

A: The timestamp on the exported file is in the format HH:MM:SS:uS. Excel does not interpret this timestamp correctly and shows the minutes as hours, the seconds as minutes and part of the uS as ms instead. If you open the csv file with a text tool or with MacOS Numbers for example, you can see the correct values. The exported file may contain more information than what can be displayed on the table like full data if the data was too large for the table cells.

FAQs on A2B Analyzer Studio & A2B Analyzer HP
---------------------------------------------

\*\* 1. Is a license required to use my hardware in A2B Analyzer Studio? \*\*

A:  No, A\ :sup:`2`\ B Analyzer Studio has removed the requirement to register your hardware. You can use A\ :sup:`2`\ B Analyzer Studio without limitations without registering it.

\*\* 2. Which firmware version should I use with A2B Analyzer Studio? And which firmware version should I use with A2B Bus Analyzer? \*\*

You must match the version of firmware with the software being used.

In A\ :sup:`2`\ B Bus Analyzer, the first two digits of the firmware must match the Software version. For example A\ :sup:`2`\ B Bus Analyzer 3.4.0 must use Firmware 3.4.x.

In A\ :sup:`2`\ B Analyzer Studio the firmware major version must be 4+ A\ :sup:`2`\ B Analyzer Studio major version and the minor versions must match. For example, A\ :sup:`2`\ B Analyzer Studio 1.0.0 must use Firmware 5.0.x

A failure of using the correct firmware results in undefined behaviour.

You can move your firmware up and down from the UI of either tool by choosing either "Update Firmware" to move to the A\ :sup:`2`\ B Analyzer Studio version or "Re-apply Firmware" to move back down to the A\ :sup:`2`\ B Bus Analyzer Firmware.

\*\* 3. Which SDK API should I use when I am using A2B Bus Analyzer Hardware from A2B Analyzer Studio? \*\*

While the API which starts by a2ba_sdk (API 1.0) is still supported, it is built on top of the new a2ba2_sdk API (API 2.0) and it is now deprecated and may be removed in a future release. We recommend that you move to use API 2.0 to ensure that you can get access to all new future fixes and enhancements going forward.

\*\* 4. What is a Personality module in A2B Analyzer HP? \*\*

Analog Devices' A\ :sup:`2`\ B Bus Analyzer HP contains 2 pluggable modules housing the A\ :sup:`2`\ B transceiver that the customer intends to emulate or monitor.

\*\* 5. Are there any restrictions on the functionality applicable for a Personality module? \*\*

A\ :sup:`2`\ B Analyzer Studio 1.0.0 restricts Personality Module 1 usage for node emulation and Personality module 2 usage for monitoring of ADAA245x transceivers.

\*\* 6. I have connected A2B Analyzer HP using a USB Type C to Type C cable but the device connect fails and the LED shows red? \*\*

When A\ :sup:`2`\ B Analyzer HP is powered using a Type C to Type C cable, ensure that the USB source is USB-PD compatible to deliver sufficient power to the device. Otherwise, we recommended that you use the external 24 V supply as well.

\*\* 7. Why does the LED show red when I connect the USB A to C cable and external power supply? \*\*

The external power supply will be evaluated only during the device connection process. If the power is sufficient, the LED will turn green, allowing the user to connect to the device. If the power is insufficient, an error will be displayed

\*\* 8. Why are the personality modules shown as unknown in the "About A2B Analyzer window" after using the analyzer as a sub node? \*\*

If the 0x50 peripheral is present in the schematic for the sub node you are emulating, it will overwrite the analyzer's personality module information. Please uncheck the **program during discovery checkbox** for the sub node peripheral you are emulating.** 9. Why is the main node audio not working when I2S1 is used and the clock drive option is enabled for I2S1? \*\*

The main node audio may not work because the clock signals BCLK and SYNC are provided by the analyzer. Enabling the transceiver clock drive option for I2S1 can cause conflicts

\*\* 10. What is the audio resolution supported over USB input and output? \*\*

=========================== ================
A2B Analyzer Studio Release Audio resolution
1.0.0 and 1.0.1             16 bit
1.1.0                       16 and 32 bit
=========================== ================

\*\* 11. What audio driver is selected for File Out? \*\*

+----------------------------+----------------+-------------+-------------+-----------------+
| Analyzer Type              | USB Resolution | Sample Rate | Driver Used | Max USB Streams |
+----------------------------+----------------+-------------+-------------+-----------------+
| A\ :sup:`2`\ B Analyzer HP | 16 bit         | 48 kHz      | MME         | 32              |
+----------------------------+----------------+-------------+-------------+-----------------+
| A\ :sup:`2`\ B Analyzer HP | 32 bit         | 48 kHz      | WDM-KS      | 32              |
+----------------------------+----------------+-------------+-------------+-----------------+

\*\* 12. Are higher/reduced sampling rates supported ? \*\*

Only 48 kHz sampling rate is currently supported

\*\* 13. Why can't I connect to my A\ :sup:`2`\ B Analyzer HP over Ethernet? \*\*

First, verify basic connectivity by pinging the device:

::

   ping <ip-address>

If the ping fails, check:

-  Network connectivity

   -  For direct static connections (PC directly to device): Use static IP addresses on the same subnet (for example PC: 192.198.1.100, device: 192.168.1.101, both using subnet mask 255.255.255.0). Configure the device's IP address and subnet mask in Network Settings and ensure your PC's network interface is configured with these on the correct Ethernet adapter.
   -  For dynamic LAN connections (both on the same network): Check Network Settings to verify the device was assigned an IP address by DHCP. If hostname resolution is not working, try using the IP address instead.

-  Firewall Settings: Check that your firewall isn't blocking the TCP connections on the configured port (default: 12345)
-  Cable connection: Ensure the Ethernet cable is properly connected and functional

\*\* 14. How do I find the IP address of my A2B Analyzer HP? \*\*

Connect via USB first and check the Network Settings in Device Properties. The assigned IP address will be displayed there. For dynamic IP assignment, it may show "--" during DHCP negotiation (up to 60 seconds).

\*\* 15. What are the default Network Settings? \*\*

The A2B Analyzer HP ships with these default network settings:

-  IP Assignment: Static
-  IP Address: 169.254.90.190
-  Subnet Mask: 255.255.0.0
-  TCP Port: 12345

To connect directly via Ethernet cable, configure your PC's network interface with a compatible IP address such as 169.254.90.100 and same subnet mask 255.255.0.0.

\*\* 16. Can I connect multiple A2B Analyzer Studio instances to the same device over Ethernet? \*\*

No, only one connection is allowed at a time. If another user is already connected, you'll be prompted to reset the device, which will disconnect the other user and allow you to connect.

\*\* 17. Why doesn't File Out work when connected via Ethernet? \*\*

File Out requires a USB connection for audio transfer to/from the host PC. When connected only via Ethernet, you'll see an error if you try to use File Out. To use this feature, connect a USB cable in addition to your Ethernet connection.

Similarly, while USB In/Out can be enabled over Ethernet, the actual audio transfer still requires a USB cable connection to your host PC.

\*\* 18. Should I use static or dynamic IP assignment? \*\*

-  **Static IP:** Recommended for dedicated test setups or direct PC-to-device connections where you want predictable addressing.

   -  **Caution:** Don't use static IP on networks with DHCP unless you coordinate with your network administrator to ensure the static IP is outside the DHCP pool range, otherwise IP conflicts may occur.

-  **Dynamic IP (DHCP):** Recommended for most LAN connections. Easier for temporary connections and networks with DHCP servers. Supports hostname resolution if your network has DNS.

   -  **Caution:** If your network has DNS and you might have more than one Analyzer on the same network, ensure you give them unique hostnames.** 19. What network port does the A2B Analyzer HP use and how to use a different one? \*\*

The device uses one configurable TCP port (default: 12345) for all communication with A2B Analyzer Studio. You can change this port in the Network Settings if needed.

When adding the device in A2B Analyzer Studio, if you've configured a custom port, you must specify it using the format <ip-address>:<port> (e.g., 192.168.1.100:54321 ). If you only provide the IP address, the default port (12345) will be used.

\*\* 20. Can I use the device over a VPN or remote network? \*\*

While technically possible, it's not recommended. VPN connections may introduce latency and instability that can cause the device to disconnect frequently. Use a direct point-to-point connection for best results.

\*\* 21. The device worked via USB but won't connect over Ethernet. What should I check? \*\*

-  Connect via USB and verify the network configuration hasn't changed since initial setup
-  Try pinging the device's IP address
-  Check if the TCP port is being blocked by network security
-  Ensure the device has power (24V supply required for Ethernet-only operation)

FAQs on A2B Bus Analyzer Unit and GUI
-------------------------------------

\*\* 1. Where can I get the licensing guide? \*\*

A: The licensing guide is available in `:doc:`/wiki-migration/resources/tools-software/a2b-bus-analyzer/licensing-guide` </resources/tools-software/a2b-bus-analyzer/licensing-guide>`__

\*\* 2. Why is the Analyzer GUI not opening? \*\*

A: Some instances may already be active or enabled remotely. Check the task manager, then remove any unneeded instances.

\*\* 3. The Analyzer GUI does not show Analyzer Device, what is the problem? \*\*

A: The A2B Analyzer device is being powered via USB which is not supported.      Please power it with the power supply that was enclosed with the product.

\*\* 4. How do I export a .dat file from Sigma studio? \*\*

A: The sigma studio schematics should be compiled, Right click the target processor, select "Export System config files". In the Bus Config File (BCF) tab, there is an option to export .dat file, select "Browse". Click "Generate" and then "Close".

\*\* 5. Is the A2B Bus Analyzer capable of streaming audio? \*\*

A: Yes, there are different ways the analyzer can stream audio. They are

-  Stream audio from the A\ :sup:`2`\ B Bus Analyzer into a file on Host PC
-  Redirect the Audio to Line Out or S/PDIF Out,
-  Record using USB Audio 2.0. For this, Windows uses ASIO, Linux uses ALSA, and macOS uses Core Audio. In order to store USB Audio you will require a third party tool. Please refer to the A2BA User Guide -> Bus Monitoring -> Audio -> Audio Stream Input/output configuration on Windows with Reaper for an example setup.

You can get the A\ :sup:`2`\ B Bus Analyzer user guide by launching the software and choose Help -> User Guide. This opens the PDF which is located in your installation.

\*\* 6. How do I know if the TDM settings are done correctly in A2B Bus Analyzer for the node emulation use case ? \*\*

A: The TDM settings on the Analyzer will be done automatically based on the settings done in A\ :sup:`2`\ B Transceiver (in Sigma Studio).

\*\* 7. Can I monitor two distinct A2B networks using a single A2B Analyzer? \*\*

A: You need two Analyzer Units to monitor events from two independent A\ :sup:`2`\ B networks.

\*\* 8. How do I run more than one instance of the A2B Bus Analyzer on the same machine? \*\*

A: Each UI must connect to a separate analyzer hardware, there is no sharing between different instances.

*Version 3.2.0 and earlier*

The A\ :sup:`2`\ B Bus Analyzer UI uses two ports: which by default are 5000 and 1880. You need to specify different ports in order to launch an additional copy of the A\ :sup:`2`\ B Bus Analyzer UI.

Note that environmental variables should be set as individual commands and not concatenated together with &.

cd "C:\\Analog Devices\\A2B Bus Analyzer" set A2B_ANALYZER_PORT=5001 set A2B_API_PORT=1881 ".\\A2B Bus Analyzer.exe"

set A2B_ANALYZER_PORT=5002 set A2B_API_PORT=1882 ".\\A2B Bus Analyzer.exe"

set A2B_ANALYZER_PORT=5003 set A2B_API_PORT=1883 ".\\A2B Bus Analyzer.exe"

*Version 3.3.0 and later*

The UI identifies that another instance of the analyzer is being run and tries the next port. The UI displays a warning message with the new port.

The UI tries a maximum of 5 ports after the initial one (5001 or the one specified in the environment variable A2B_ANALYZER_PORT). If you require more than 5 UIs at the same time set up environment variables defining ports for all those instances.

\*\* 9. In version 3.2.0 and later, if I have enabled USB Out and then I enable File Out I can see a glitch in my USB Out, why is this? \*\*

From version 3.2.0, File Out captures audio directly from the USB audio device rather than the USB communication device. When File Out is started, any streams that were already sinking on USB need to be stopped so that the USB route can be restarted with the additional streams that were requested for File Out. Once File Out is started, you cannot alter the output of the USB channels (the UI disables USB controls and the SDK will return A2BA_SDK_ROUTE_IN_USE). When File Out is stopped the original USB route is reinstated.

\*\* 10. Why is the dat generated with File Out in version 3.1.0 and in 3.2.0 different? \*\*

File Out in version 3.1.0 recorded audio as floating point, from version 3.2.0 it is recorded as signed 32 bit integer.

\*\* 11. I can no longer connect to the UI/SDK after updating my firmware from 3.1.x. Is my analyzer bricked? \*\*

In 3.2.x the A2B Bus Analyzer Firmware started to support standalone mode. This means that the position of the standalone switch, which was ignored before, is really important. When the switch is up (standalone enabled) the device can no longer connect via USB and therefore it looks like it is bricked. If you turn standalone off then the UI and SDK should be able to connect in a similar manner as in 3.1.0 and earlier. If standalone switch is enabled then the status LED is always red and the A2B Bus Analyzer UI and SDK cannot discover the device.

\*\* 12. I am not able to save audio file using 'File Out' option, why am I getting an error? \*\* |image1|\


|image2|

A: Please ensure you have write permissions to save the audio file.

When attempting to use File Out from Remote Desktop on Windows, the USB Audio device is not present in Device Manager and is therefor not available to record audio from. From the SDK, the a2ba_sdk_monitor_audio_file_out_start and a2ba_sdk_emulator_audio_file_out_start APIs will return A2BA_SDK_FEATURE_INACTIVE. From the UI, enabling File Out shows an error message.

In order to get file out to work correctly you must set the  Remote Audio Settings in the Local Resources tab in the Remote Desktop Connection options to "Play on remote computer" before connecting to the remote machine. For more detailed steps see "Bus Monitoring > Audio > Audio Traffic and Controls > Recording Audio to File" in the User Guide.

Refer to :doc:`Known Issues </wiki-migration/resources/tools-software/a2b-bus-analyzer/known-issues>` number 2390: File Out in version 3.2.0 of the A\ :sup:`2`\ B Bus Analyzer does not work in non-English language devices, please update to 3.3.0.

\*\* 13.  How do I detect if the firmware is operating as intended? \*\*

Please refer to the below image.


|image3|

The Analyzer board has two LEDs, "Power", which should always be green when the board is powered and "Status". When the firmware is operating correctly the Status LED should be illuminated green as shown by the bottom A\ :sup:`2`\ B Bus Analyzer in the above image.

If the "Status" LED is not green and you are using Windows then please check if the A\ :sup:`2`\ B Bus Analyzer is getting enumerated in the Ports (COM & LPT) section of Device Manager.

If the Status LED is RED, disable the standalone mode. Version 3.2 and later of the firmware supports standalone mode. While the standalone mode switch was ignored in the past, if standalone is enabled (switch up), the firmware now disables USB and therefore the UI/SDK cannot connect to it. Please disable the standalone (switch down) on the emulator side of the A\ :sup:`2`\ B Bus Analyzer.

\*\* 14. What is the maximum size of an audio file that can be saved using the A2B Bus Analyzer? \*\*

The maximum size of a WAV file is approximately 4GB. Should you require a larger file size consider saving using a ".raw" file extension which has no limit, it will save the audio as raw signed 32 bit integer PCM data.

\*\* 15.  What are the different recording options available and how many channels can be recorded? \*\*

There are two options to record a audio file

-  File Out
-  USB Out

The A\ :sup:`2`\ B Bus Analyzer UI supports saving a maximum of 32 audio streams per session.

With the USB output, we can capture and record network audio on the host computer. Various third-party software applications can be used, e.g. Audacity or REAPER. Both applications are available for Windows, Mac, and Linux.

Refer to the A2B_Analyzer_User_Guide for more information.

\*\* 16.  What are the different audio sources available on the A2B Bus Analyzer? \*\*

There are four methods to provide input to the A\ :sup:`2`\ B Bus Analyzer

-  Line In
-  S/PDIF In
-  File In
-  USB In

Various test tones can also be generated to provide controlled and predictable signals in testing and demonstrations.

The A2B Bus Analyzer Application supports three different test tones:

-  Sine wave
-  White noise
-  Pink noise

Refer to the section 'Source Audioâ€”Input Controls' in 'A2B_Analyzer_User_Guide' for more information.

\*\* 17.  Why is there a power mismatch error when I am emulating AD2428? \*\*

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/2x_power_error.png
   :width: 400px

A: The issue is due to incorrect export in the .dat file from SigmaStudio for AD2428 devices on SS v4.6 and A2B DLL v19.9.0. But this is corrected in A2B DLL v19.10.0.

\*\* 18. Is there an ASIO driver for the ADI A2B analyzer? \*\*

A: ASIO driver is required only if we want to use analyzer's USB IN/OUT and FILEOUT features.

Please refer the "Recording audio via USB output" section in analyzer user guide to configure the ASIO driver. You can get the A\ :sup:`2`\ B Bus Analyzer user guide by launching the software and choose Help -> User Guide.

\*\* 19. I get an error that tells me to configure the analyzer port environment variable (A2B_ANALYZER_PORT), why? \*\*

A: The A2B Bus Analyzer requires a port on your computer for internal communication. If the analyzer detects that this port is used by another application (or another instance of the A2B Bus Analyzer) it tries to move to the next port up to 5 times. If the A2B Bus Analyzer cannot find an available port after this, the analyzer requires that you set up the environment variable A2B_ANALYZER_PORT to another value from which to start looking, for example 5006. To do this please refer to how to set up environment variables in your OS of choice. To know if your computer is using port 5001 you can run the command netstat -abon and search for 5001 in your results.

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/port_error.png
   :width: 400px

set up an environment variable globally for a windows.

\*\* 20. Why there is no audio data using the A2B Bus Analyzer as Monitor? - Part 1 ENDSNIFF \*\*

A: To sniff the monitor slots, bus monitor should be enabled by the main node.To enable, please set the below bit in SigmaStudio schematic before discovery (A2B_DATCTL.ENDSNIFF).

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/endsniff.png
   :width: 400px

\*\* 21. Why there is no audio data using the A2B Bus Analyzer as Monitor? - Part 2  \*\*

A: If no audio data (upstream/downstream) is present between the nodes where the Analyzer is connected for monitoring, then the analyzer will not show any audio traffic.

**Example:** Assuming the A2B network contains two sub nodes where the audio stream is configured only between sub0 & sub1 and if the Analyzer(monitor) is connected between

main node & sub0 then the analyzer software will not show any audio traffic.

Before starting the discovery, ensure that the "start capturing" button is enabled in the analyzer UI. Please follow the following steps to enable bus monitoring:

-  Activate bus monitoring by sliding the Enable Switch
-  Click Play to start capturing audio and event traffic on the bus.

**         Note:** Event and Audio Traffic area will remain blank until the network host initiates A2B discovery.

-  Switch to the A2B network host program (e.g., SigmaStudio, Head-unit software) and initiate discovery process.
-  With network discovery initiated on the host, the A2B Bus Analyzer application will begin capturing event and audio traffic.

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/monitor_start.png
   :width: 400px

You can also enable monitoring of the network, which is already discovered, refer the "Enable monitoring of network already discovered" section in the analyzer user guide,

which is available at Help -> User Guide. This opens the PDF which is located in your installation.

**Note: **\ When you use the Analyzer as a only monitor, removing the power supply to the analyzer will stop the monitoring but keep the A2B Network alive.** 22. Does the loopback feature of the Analyzer support routing a generated tone through an external development board? \*\*

A:  The loopback feature of the Analyzer doesn't support routing the sine tone to external board. It is an internal loopback feature only.

Below is the example for performing external Loopback, Consider A2B Network with 1Main Node (Analyzer) and 1Sub node (Sub0)

A2B Analyzer (Main Node) -> Generate Sine Tone -> Send Sine Tone down stream using stream configuration -> Connect Line-Out of Sub0 to Line-In (External Loop Back) -> Send the processed Sine Tone to the Main Node using stream configuration.

\*\* 23. How do I access the Analyzer on a remote PC through remote desktop? Can I still use File Out? \*\*

A: The default settings for a remote desktop session restrict access to audio input/output devices. By changing the settings of the remote desktop session, the Analyzer should be listed in the device manager.

Follow Below steps:

-  Open Remote Desktop Connection
-  Before connecting, click “Show Options.”
-  Click the “Local Resources” tab.
-  In “Remote audio”, click “Settings…” to “Configure remote audio settings.”
-  Then in the “Remote audio playback” section, select “Play on remote computer” (see image below)

|resources-tools-software-a2b-bus-analyzer-remote_desktop.png|

- Click OK and connect to the remote desktop.

\*\* 24. Why is the A2B Bus Analyzer not being detected on the host PC? \*\* A: Ensure that standalone mode is disabled by having the Standalone switch down and reload the list of devices in the UI.

Make sure that the analyzer is connected via its USB port to the PC and ensure that the analyzer is powered with a power supply, not via USB.

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/analyzer_usb_connection.png
   :width: 400px

If you are on windows, check that the A2B Bus Analyzer is getting enumerated in the device manager.

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/device_manager.png
   :width: 400px

Press the refresh button to show the available analyzer devices connected to the PC.


|image4|

Select the analyzer device from drop down and press the power button to connect.


|image5|

If the problems still persist, please launching the Analyzer software from the command line and send us any errors that may appear on the console when you are trying to connect to the device. The analyzer is by default at:

::

    "C:\Analog Devices\A2B Bus Analyzer\A2B Bus Analyzer.exe"

\*\* 25. Why do I not see streams when .DAT file is exported with 32 streams configured? \*\*

A: .DAT file export from SigmaStudio/SigmaStudio+ is incorrect when 32 streams are configured, as the slot registers are not correctly updated. Update the slot registers manually in SigmaStudio/SigmaStudio+ and then export .DAT file to work with 32 streams.

**27. How to transmit command packets to A2B Analyzer using SigmaStudio 4.6/4.7?**

A: User can utilize the USBi at the Bus Monitor side of A2B Analyzer to transmit packets.

-  Connect USBi to the Bus Monitor side.

-  Launch SigmaStudio 4.7/4.6 and open any example project.

-  Right-click on the PC target processor, then navigate to *Device Properties -> Direct Programming* *window*.

-  Choose the configuration xml file and click on the program button. This will transmit the commands through USBi to the Analyzer.

**28. Is it possible to use more than 4 different test tones?**

A: In the A2B 1.0, max of 4 different tones support, but the customer can achieve 16 different tones by using the USB-In option with the help of Reaper tool.

**29. Does Opto A2B support SPI data transfer?**

A: For Opto A2B [A2B plugin 1.3.1], SPI is not supported. Only I2C protocol is supported at the moment.

**30. When using AD24xx series in A2B Analyzer, can one configure all 32 channels in one direction (upstream/downstream) at same time?**

A: When you configure 32 channels, in monitor you can see only 30 because a maximum of 30 channels can be used in one direction, two channels are reserved for events and traffic.

This is applicable for both AD242x and AD243x series.

**31. How can one convert the audio signal seen in A2B Analyzer UI to RMS level (dB)?**

A: We can convert the audio signal in the UI to Level (dB RMS) using the following approach.

The RMS level(dB) can be calculated by:

1.Computing the moving average of the square of the audio samples.

2.Performing a square root of the average to get the RMS value.

3.Computing 20 log(RMS) to get the RMS value in DB.

**32. When computing the moving average of the square of the audio samples, how many audio samples are used to calculate one average of the square?**

A: The First order Low Pass Filter is used to calculate the running average of the samples.

**33. How many unique tones can I route in A2B Bus Analyzer?**

A: A2B Bus Analyzer currently support 4 unique tones.

**34. How can I route more than 4 unique tones in A2B Bus Analyzer?**

A: We recommend using USB In option to feed in up to 32 unique tones. Tools like Reaper/ Audio Precision Analyzer can be used to flexibly route unique tones.

**35. what sample rate does A2B Bus Analyzer support?**

A: Analyzer only supports 48kHz sample rate.

**36. Why does the A2B Bus Analyzer read the AD2433 transceiver ID when emulated as the 2428 will this affect the discovery flow?**

A: The A2B Bus Analyzer contains an AD2433 transceiver. As a result, even when emulating the Analyzer as a 2428, the transceiver ID that is read will still be AD2433. However, this behavior does not pose any issues and will not affect the discovery flow.

**37. How many pairs of opto branches does the A2B Bus Analyzer support?**

A: The Analyzer supports only one pair of opto branches.

**38. How does the A2B Bus Analyzer GUI display the write sequence?**

A: **A2B Bus Analyzer Monitor GUI**:

The Monitor GUI presents the write sequence, displaying the Peripheral Address, followed by the target address where the write operation is being performed, and concluding with the value being written, as illustrated in the image below. Let's take the example of writing

::

       Data 0x0F to the 0x4000 address of Peripheral 0x39.

Below is how this action would be displayed in the Monitor GUI.

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/monitor_sequence.png

**A2B Bus Analyzer Emulator GUI**:

Analyzer functions as a Main Node Emulator, the write sequence is displayed as the address in the address width, followed by the data.

::

            Peripheral Chip Address: 0x39
            Register Address: 0x4002
            Address Width: 2

.. image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/emulator_sequence.png
   :align: center

In the above example, you can see 0x40 0x02 as the register address (split based on address width), followed by Data (0x00, 0xFD, 0x00, 0x0C, 0x20, 0x03).

**39. What is the measured latency between the Main Node and Sub Node for both analog and SPDIF connections using Audio Precision?**

A: **Latency Measurements**:

System Latency measured between Main Node Analyzer connected to Sub Node Analyzer Using Audio Precision:

::

     1.Latency between Analog IN at the Main Node (from AP) to Analog OUT at the Sub Node (captured in AP)
     2.Latency between SPDIF IN at the Main Node (from AP) to SPDIF OUT at the Sub Node (captured in AP)

Both measurements yielded a latency of approximately **~23ms**.

**40. What steps should be followed to interface the A2B Bus Analyzer with Audio Precision (AP)?**

A: A2B Bus Analyzer is an ASIO-compliant device.

1.Download and install the ASIO4ALL driver from https://asio4all.org/about/download-asio4all.

2.In Audio Precision, choose ASIO under the Input/Output Configuration. //


|image6|

//

3.Check the settings pane to ensure that the ADZS-A2B-ANALYZER ASIO input/output channels are properly mapped to the APx input/output channels.

4.Enable the “USB” output in the ADZS-A2B-ANALYZER UI

5.Once enabled, all channels will be available to ASIO, and Audio Precision can directly access these channels from Windows ASIO.

**41. Is peripheral programming via SPI supported?**

A: Peripheral programming via SPI is not supported.

**42. Is PDM emulation possible in A2B Bus Analyzer?**

A: True PDM emulation is not supported in Analyzer. We recommend setting Rx Pins as an alternative and send microphone data over USB using applications like Reaper/Audacity, while using A2B Bus Analyzer.

**43. Can A2B Bus Analyzer be powered directly from car battery?**

A: Yes, the Analyzer can be powered from a car battery(12V), for safer side we recommend not to connect during ignition start/stop.

FAQs on License
---------------

\*\* 1. Do I need to license my hardware? \*\*

A: When using A2B Bus Analyzer license checking is performed and therefore you will need to follow the licensing workflow in your GUI. A2B Analyzer Studio removed the requirement to have your license key added to your hardware. If you are intending to use both products, you must install the license from A2B Bus Analyzer

\*\* 2. What can I do if I am connected to VPN and my license activation is failing? \*\*

A: Under certain rare circumstances the license activation mechanism may fail if the computer is connected to a VPN. In case of problems you should disconnect their VPN. If you are still having issues after that, please get in touch with a2b.analyzer.support@analog.com.

\*\* 3. I re-flashed my analyzer board and now the tools report that I do not have a license. What can I do? \*\*

A: Re-flashing the board removes the license information which was stored at the time of license activation. You should have an email from the activation time that contains both your license key and the validation code. You can use that data again on the same board by utilizing the "Offline activation" feature. If you no longer have your validation code, please request the validation code again from the support team at a2b.analyzer.support@analog.com making sure that you inform them of your device's Hardware Serial Number.

\*\* 4. In how many computers can I use an analyzer with the same license? \*\*

A: The A\ :sup:`2`\ B Bus Analyzer license is related to the A\ :sup:`2`\ B Bus Analyzer board and not to the tools or computers. This means that once a license is linked with the specific board, any number of computers (Windows/MacOS/Linux) can use it, there are no limitations.

\*\* 5. Where should i put Activation key for "offline" activation option?*\*

A: Please refer to our :doc:`licensing guide </wiki-migration/resources/tools-software/a2b-bus-analyzer/licensing-guide>`.

FAQs on User Accessible SDK (UASDK)
-----------------------------------

\*\* 1. What is the SDK? \*\*

A: The SDK (Software Development Kit) is a set of APIs which provide programmers with a C and Python interface to directly access the A\ :sup:`2`\ B Analyzer HP and A\ :sup:`2`\ B Bus Analyzer programmatically without using the UI. This means that the SDK allows users to set up automated testing where control via a GUI is not practical. For some APIs the SDK provides more control than the UI. Please refer to the html documentation for more details on each API. This is located in your exported SDK folder at ``uasdk/C/docs`` or ``uasdk/python/docs``. You can also launch the API documentation from the UI from the Sdk menu.

\*\* 2. Where can I get the UASDK? \*\*

A: The UASDK comes bundled as part of the A\ :sup:`2`\ B Bus Analyzer GUI software and must be exported to your computer before it can be used. To export your SDK:

In A\ :sup:`2`\ B Analyzer Studio and A\ :sup:`2`\ B Bus Analyzer version 3.4.0 or later: In Menu Sdk → Export User-Accessible SDK… from the application's menu bar.

In A\ :sup:`2`\ B Bus Analyzer version 3.3.0 or earlier: In Menu Help → Export User-Accessible SDK… from the application's menu bar.

This generates a self-contained ``uasdk`` folder which contains the SDK itself as well as examples and API documentation.

\*\* 3. What is the functionality supported by UASDK? \*\*

A: The SDK contains C/C++ and Python interfaces to run certain A\ :sup:`2`\ B Bus Analyzer commands like connect to a device and get audio levels. The SDK supports both bus monitor and node emulator. For detailed information about the APIs supported see the HTML documentation in your exported SDK folder at ``uasdk/C/docs`` or ``uasdk/python/docs``. You can also launch the API documentation from the UI from the Sdk menu.

The SDK also contains a set of examples to demonstrate basic functionality. You can refer to the SDK user guide to build and run the examples. The SDK User Guide can be found as PDF in the exported SDK. You can also launch the PDF from the UI on version 3.4.0 or later from the Sdk menu. Finally it can also be found at :doc:`A2B Analyzer Studio SDK User Guide </wiki-migration/resources/tools-software/a2b-analyzer-studio/sdk-user-guide>` and :doc:`A2B Bus Analyzer SDK User Guide </wiki-migration/resources/tools-software/a2b-bus-analyzer/sdk-user-guide>`.

\*\* 4. Where can I get the UASDK User guide? \*\*

A: From release 3.4.0 there is a dedicated Sdk menu in the A2B Bus Analyzer UI. Please choose Sdk→SDK User Guide. In all versions you can access the SDK user guide in your exported SDK in the folder uasdk. You can also get the SDK user guide at at :doc:`A2B Analyzer Studio SDK User Guide </wiki-migration/resources/tools-software/a2b-analyzer-studio/sdk-user-guide>` and :doc:`A2B Bus Analyzer SDK User Guide </wiki-migration/resources/tools-software/a2b-bus-analyzer/sdk-user-guide>` in this wiki.

\*\* 5. Which version of python is supported by the SDK? \*\*

A: A\ :sup:`2`\ B Analyzer Studio includes a python wheel that can be used in python 3.9 or later (tested up to 3.12). In A\ :sup:`2`\ B Bus Analyzer there are two versions of the User-Accessible SDK Python Wheel for each OS, Python version 3.8 and version 3.10

\*\* 6. Can a device be used from the UI and the SDK at the same time? \*\*

A: The shared libraries used by the SDK and the UI are completely separate and they do not interact. This means that when trying to use the same device, both the UI and the SDK try to use the same USB which results in a failure.

\*\* 7. Is it possible to use the monitor and the emulator at the same time with the SDK? \*\*

A: Yes, you can configure both monitor and emulator at the same time with the SDK.

Note: Your exported SDK contains monitor and emulator basic examples and documentation about the APIs.

\*\* 8. Which APIs can be used for selecting Stream Id and starting a tone with that Stream Id? \*\*

A: You can use the APIs ``a2ba2_sdk_emulator_audio_info()/a2ba_sdk_emulator_audio_info()`` for requesting the audio info (stream Info) from the emulator and ``a2ba2_sdk_sdk_emulator_tone_start/a2ba_sdk_emulator_tone_start (streamId, pToneCtlInfo)`` for starting the tone where you can pass the streamID as a parameter. In A2B Bus Analyzer release 3.4.0 and later you can also use ``a2ba_sdk_emulator_tone_ex_start`` to be able to provide multiple streams.

For more details, you can refer to the SDK API documentation in C/C++ or Python. You can access the documentation in your exported SDK in the uasdk folder or from the UI in the Sdk menu. The SDK also contains a file out example in C++ and python which shows how to configure a test tone.

\*\* 9. Where can I get the A2B SDK DLL to use A2B Analyzer with LabVIEW platform? \*\*

A: Refer to the following link for calling DLL from LabVIEW application. `Call a Dynamic Link Library (DLL) from LabVIEW (NI website) <https://knowledge.ni.com/KnowledgeArticleDetails?id=kA03q000000YGggCAG&l=en-US>`_

``a2ba_sdk.dll`` can be found in the path: uasdk\\C\\lib

To export your SDK

In A\ :sup:`2`\ B Analyzer Studio and A\ :sup:`2`\ B Bus Analyzer version 3.4.0 or later: In Menu Sdk → Export User-Accessible SDK… from the application's menu bar.

In A\ :sup:`2`\ B Bus Analyzer version 3.3.0 or earlier: In Menu Help → Export User-Accessible SDK… from the application's menu bar.

\*\* 10. is it possible to send mailbox command from A2B Bus Analyzer itself without using the SDK (C++ or Python)?*\*

A: It is possible to send mailbox command from the UI in version 3.4.0 or later. For all versions is is possible to do it in C++/Python.

FAQs on Standalone Mode
-----------------------

\*\* 1. Is there a limitation on how much data can be captured during Standalone Mode? \*\*

A: There is no restriction on the duration of the standalone mode that can be captured as long as there is storage space left in the SD card. However a maximum of only 8 audio slots can be captured. You can refer to the user guide Standalone mode section (8.5) for more details. The user guide is in PDF form in your installation.

\*\* 2. The Standalone session playback doesn't show levels for audio slots. Why? \*\*

A: Audio is captured only for a trigger window during the Standalone mode. For successful audio capture, make sure of the following

-  The network is discovered
-  ENDSNIFF bit of DATCL register is written by the Main Node
-  The trigger Switch is pressed
-  The maximum number of triggers has not exceeded the count of 25

You can refer user guide Section "Data capture during standalone mode" for more details.

\*\* 3.Q: I turned Standalone Mode (Switch up) and now my A2B Bus Analyzer is not booting up and my Status LED is red. Why? \*\*

A: In standalone mode, the device will not connect to PC and will operate in standalone mode and record the events in SD card. This is ideal for vehicle recording sessions where PC intervention is not preferred. Once the recording session completes, you must switch-off the standalone mode and enter “standalone retrieval mode” in the UI by checking “BMSA” checkbox in the monitor UI. Then the application will retrieve the recorded contents and starts displaying the events and audio.

Refer to the detailed documentation in the User Guide on BMSA (bus monitor standalone mode) which is available in Help → User Guide. This opens the PDF which is located in your installation.

FAQs on Advanced Use cases
--------------------------

\*\* 1. How can I specify which Windows audio driver is used to save audio to a file? How can I capture all 32 bits? \*\*

A: The A2B Bus analyzer uses the PortAudio library to record from USB to a file. PortAudio supports different Windows drivers each of which drivers has different characteristics. While the A2B Bus Analyzer by default uses the default driver (commonly MME), you can define environment variables to select the driver most appropriate for your use.

See below table for a description of the host APIs commonly available on Windows.

+---------------+-----------------------+-------------------------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| Host API Name | PortAudio Host API ID | Bits captured out of 32 | Description                                                                                                                                                                                                                                                           |
+===============+=======================+=========================+=======================================================================================================================================================================================================================================================================+
| MME           | 2                     | 16                      | Usually the default Host API used. It truncates audio to 16 bits and pads the other 16 bits with zeros. Monitor, Emulator and other applications may use the device to record audio at the same time.                                                                 |
+---------------+-----------------------+-------------------------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| DirectSound   | 1                     | 16                      | Truncates audio to 16 bits and pads the other 16 bits with zeros. Monitor, Emulator and other applications may use the device to record audio at the same time.                                                                                                       |
+---------------+-----------------------+-------------------------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| WASAPI        | 13                    | 24                      | Bit exactness appears to vary between 24 and 32 bits. Performance is unreliable and Monitor, Emulator and other applications may interfere with one another.                                                                                                          |
+---------------+-----------------------+-------------------------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| WDM-KS        | 11                    | 32                      | Records all 32 bits but may only record from one of Emulator, Monitor or another application at a time.                                                                                                                                                               |
|               |                       |                         | When this driver is used PortAudio cannot differentiate between Analyzers so only one should be plugged in.                                                                                                                                                           |
|               |                       |                         | As well as setting ``A2BA_HOST_API``, this driver requires the environment variable ``A2BA_FILE_OUT_DEVICE`` which may vary between Windows machines. Please get in touch with a2b.analyzer.support@analog.com for help with what to set ``A2BA_FILE_OUT_DEVICE`` to. |
+---------------+-----------------------+-------------------------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

.. |image1| image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/unable_to_capture_fileout.jpg
   :width: 400px
.. |image2| image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/unable_to_capture_fileout1.png
   :width: 400px
.. |image3| image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/firmware.jpg
   :width: 400px
.. |image4| image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/refresh.png
   :width: 600px
.. |image5| image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/start_button.png
   :width: 600px
.. |image6| image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/asio4all.png

.. |resources-tools-software-a2b-bus-analyzer-remote_desktop.png| image:: https://wiki.analog.com/_media/resources/tools-software/a2b-bus-analyzer/remote_desktop.png
